# Replace Block 2 in 需求确认.md
import sys
sys.stdout.reconfigure(encoding='utf-8')

filepath = 'v:/Default/Desktop/当前学习内容/寻找导师的邮件/李自翔/量子蒙特卡洛/pilot-test/计划/需求确认.md'

with open(filepath, 'r', encoding='utf-8') as f:
    content = f.read()

start = content.find('## Block 2: 工具设计')
end = content.find('## Block 3: 执行规范')

new_block = """## Block 2: 工具设计

### 2.1 整体工作流与技术概览

#### 2.1.1 用户使用流程

用户提供导师信息，AI 驱动的流水线自动执行：

```
第0步 —— 用户输入
  提供：导师姓名 + 学校/机构 + 机构官网导师URL
  可选补充：方向关键词、已知论文列表

第1步 —— 身份验证（AI自动执行）
  浅调研 → 输出 00_身份验证卡.md
  用户读卡 → 确认"是这个人"或"不是"
  确认后进入下一步，否则调整输入重试

第2步 —— 基础画像（AI自动执行）
  深调研 → 输出 01_基础画像.md
  用户可选修改 → 进入下一步

第3步 —— 领域脉络（AI自动执行）
  分析 → 输出 02_领域脉络.md
  用户可选修改 → 进入下一步

第4步 —— 论文定位（AI自动执行）
  分析 → 输出 03_论文定位.md
  用户可选修改 → 进入下一步

第5步 —— 学习讲义（AI自动执行）
  设计 → 输出 04_学习讲义.md
  用户终审
```

**核心规则**：用户可在任意步骤后介入修正。每步完成后 AI 自检（来源可验证、信息一致），自检不通过则不进入下一步。

#### 2.1.2 技术栈概览

| 层级 | 选型 | 说明 |
|:-----|:-----|:------|
| 执行引擎 | Claude Code Skill（SKILL.md） | 流水线指令写在 SKILL.md 中，AI 按步骤执行 |
| 数据采集 | 免费学术 API（OpenAlex/Semantic Scholar/ORCID/arXiv） | 零费用，无需 API Key |
| 页面抓取 | MCP HTTP fetch / Tavily Extract | 读机构官网、中文百科、新闻页面 |
| 身份消歧 | ORCID API 三级搜索（名称+机构+email） | 配合官网论文列表交叉比对 |
| 信息搜索 | Web Search / Exa / Serper | 中文网络信息（新闻、采访、知乎） |
| 输出格式 | Markdown + YAML frontmatter | 人可读、机器可索引 |
| 辅助脚本 | Python（可选） | fetch_papers.py / build_index.py |

**不引入**：Web 服务器、数据库、Docker、任何需要部署的组件。

#### 2.1.3 流水线全貌

五份文件，从验证到讲义逐层递进。前一段的输出为后一段提供认知上下文，信息量逐段收窄、理解深度逐段加深。

```
                     ┌──────────────────────┐
    用户输入 ──────→ │ 00_身份验证卡.md       │ ← Phase 1a
                     │（确认身份后再继续）     │
                     └──────────┬───────────┘
                                │ 用户确认"是"
                     ┌──────────▼───────────┐
                     │ 01_基础画像.md        │ ← Phase 1b
                     │（完整学者 profile）    │
                     └──────────┬───────────┘
                                │
                     ┌──────────▼───────────┐
                     │ 02_领域脉络.md        │ ← Phase 2
                     │（领域发展树+里程碑）    │
                     └──────────┬───────────┘
                                │
                     ┌──────────▼───────────┐
                     │ 03_论文定位.md        │ ← Phase 3
                     │（论文时间线+位置标注）  │
                     └──────────┬───────────┘
                                │
                     ┌──────────▼───────────┐
                     │ 04_学习讲义.md        │ ← Phase 4
                     │（从零到前沿学习路线）   │
                     └──────────────────────┘
```

#### 2.1.4 关键设计原则（全工具通用）

1. **所有信息必须标注来源 URL**。不标注来源的信息不写入产出。
2. **缺失比编造好**。API 返回为空 → 标注"未找到"，不生成 AI 推断。
3. **交叉比对**。同一条信息至少 2 个来源时才写入"已验证"；单一来源标注"仅单源"。
4. **多源冲突**。列出多个来源的不同信息，不加 AI 仲裁定论。
5. **用户确认前置**。身份消歧不允许全自动——必须输出验证卡等人确认。
6. **每段自检**。每段产出后 AI 检查：所有外部引用是否有 URL、是否有事实性矛盾。

---

### 2.2 第1段：身份验证与基础画像

#### 2.2.1 工作流程（自然语言描述）

第1段是整个流水线的入口。用户给了"张鹏举 + 中科院物理所 + 官网 URL"后，AI 需要做两件事：

① **确认这是谁**——官网 URL 指向的页面是可靠的"身份锚点"，从页面提取的邮箱、论文标题、研究方向可以用于在学术数据库中锁定唯一 ID。这一步不可全自动，必须让用户确认。

② **画出完整画像**——确认身份后，用唯一的 Author ID 从各平台拉取所有公开数据，组织成结构化画像。

两段之间插了一个"人闸"：AI 先做浅调研输出验证卡，用户看一眼说"是这个人"后才做深调研。

#### 2.2.2 技术原理与详细执行

**身份消歧的核心原理**：

学术界通用的消歧方法（OpenAlex/Semantic Scholar/AMiner 用的）是基于 ML 聚类——把同名作者按合作者网络、机构归属、研究主题一致性等特征归类。但我们的场景有独特的优势：用户提供了机构官网 URL。这个 URL 指向的老师是确定的，不需要靠聚类猜。

所以我们用的消歧方法更简单可靠：**用官网 URL 提取的论文标题做 ground truth，跟 API 返回的候选人的论文列表做直接比对**。匹配度高的就是本人。不需要任何 ML 模型。

**Phase 1a：身份验证（6 步）**

```
Step 1 —— 抓机构官网页面
  方法：Tavily Extract 或 HTTP fetch 读取 URL 页面内容
  提取：姓名（中/英）、职称、邮箱、研究方向、论文标题列表、教育/工作经历（如有）
  异常处理：页面无法访问 → 告知用户确认 URL
            页面无论文列表 → 跳过论文比对步骤

Step 2 —— 搜索 ORCID
  方法：ORCID Public API v3.0 expanded-search
        URL: https://pub.orcid.org/v3.0/expanded-search?q=email:{邮箱}
        或 q=given-and-family-names:{姓名}+affiliation-org:{机构}
  返回：候选人列表（含 ORCID iD、name、institution-name）
  异常处理：返回 0 人 → 未注册 ORCID，跳过

Step 3 —— 搜索 OpenAlex
  方法：OpenAlex /authors 接口
        URL: https://api.openalex.org/authors?search={姓名}&filter=last_known_institutions.id:{机构 ROR}
        中科院物理所 ROR: https://ror.org/05qbk4x57
  返回：候选作者列表（works_count, cited_by_count, institution）
  异常处理：中文名 URL 编码问题 → 改用英文拼音重试

Step 4 —— 搜索 Semantic Scholar（辅助）
  方法：Semantic Scholar /author/search
        URL: https://api.semanticscholar.org/graph/v1/author/search?query={姓名拼音}
  返回：候选作者列表（paperCount, citationCount）

Step 5 —— 论文标题交叉比对（核心消歧手段）
  方法：Step1 官网论文标题 → 与每位候选人的论文列表做标题匹配
  判定标准：
    >=80% 命中 → 锁定，高置信度
    50%-80% → 最可能候选人，标注置信度
    <50% → 多个候选人部分匹配 → 让用户选择

Step 6 —— 输出 00_身份验证卡.md
```

**Phase 1b：基础深调研**

```
用 Step5 锁定的唯一 Author ID 拉取完整数据：

OpenAlex:  /authors/{id} → 职业信息、引用总数
           /authors/{id}/works → 完整论文列表（按引用数降序）

Semantic Scholar:  /author/{id}/papers → TLDR 摘要、领域标签

arXiv API:  search?query=au:{姓名} → 最新预印本

Web Search: "姓名 + 机构 + 新闻/采访/讲座" → 公开多维信息

整理 → 输出 01_基础画像.md
```

**从实际测试跑张鹏举所得的重要经验**：

1. **OpenAlex 对中文名消歧不够可靠**。同名跨多机构、无清晰主条目的情况常见。不能单靠 OpenAlex 返回做决定，必须用官网论文交叉验证。
2. **邮箱是 ORCID 搜索的黄金锚点**。ORCID expanded-search 支持按 email 搜索，有邮箱则命中率极高。
3. **Semantic Scholar 在物理领域的论文覆盖率不如 OpenAlex**。建议 OpenAlex 为主、S2 为辅。
4. **arXiv API 对中文名搜索差**。arXiv 上中文作者名通常是拼音，直接匹配困难。

#### 2.2.3 输出规格

**00_身份验证卡.md** — 半页，现场出结果，供用户确认：

```
══════════════════════════════════════════════════════════
  身份验证卡 — 请确认是否为您要调研的老师

  用户提供的原始信息:
    姓名:   张鹏举
    机构:   中科院物理所
    官网:   https://edu.iphy.ac.cn/detail_teacher.php?id=6368

  从页面提取的核心身份信息:
    姓名:   张鹏举 (Pengju Zhang)
    职称:   特聘研究员，博导
    邮箱:   pengju.zhang@iphy.ac.cn
    机构:   中科院物理所 · 超快物质科学中心

  学术数据库身份标识:
    ORCID:       未匹配到注册
    OpenAlex ID:  A5031655767
    S2 ID:        2400405493

  论文标题比对: 官网 3 篇 vs 候选 3 篇 → 匹配度 100%

   ⚠️ 请确认这是您要调研的老师: [yes / no]
══════════════════════════════════════════════════════════
```

**01_基础画像.md** — 9 节，每节字段固定，无数据留空不编：

```
Frontmatter（机器可读）:
  affiliation / department / tags / source_updated /
  orcid / openalex_id / s2_id（如有）

1. 身份标识 — 姓名（中/英）| 职称 | 机构 | 部门 | 邮箱 | 主页 | 各平台 ID

2. 学术履历时间线
   教育：年份 → 学校 → 学位
   工作：年份 → 机构 → 职位
   每条标注来源 URL

3. 研究方向
   3a. 总体概览（外行能看懂的一段话）
   3b. 具体方向（分点，每点 1-2 句）

4. 代表性论文
   表格：序号 | 标题 | 期刊 | 年份 | DOI | 引用数 | 一句话说明
   分"近期工作"和"高引工作"两组

5. 学术影响力指标
   总引用数 / h-index（如有） / 人才项目 / 获奖

6. 合作网络
   博导 / 博后老板 / 高频合作者

7. 公开多维信息
   新闻 / 采访 / 讲座 / 学术报告（含来源 URL）

8. 技术关键词
   按类别分组：实验技术 | 理论工具 | 研究对象 | 应用方向

9. 来源说明与多源比对
   每条信息的 URL 出处 / 一致性表 / 缺失信息标注
```

---

### 2.3 第2段：领域发展调研（待填充）

#### 2.3.1 工作流程

（待设计 — 核心问题：如何从第1段的论文列表+研究方向标签出发，追溯该子领域的发展脉络？）

#### 2.3.2 技术原理与执行

（待设计 — 需确定：引文网络回溯策略、里程碑论文筛选标准、领域树构建方法）

#### 2.3.3 输出规格（02_领域脉络.md）

（待设计 — 预期包含：领域发展树、里程碑论文表格、当前前沿定位）

---

### 2.4 第3段：论文串联定位（待填充）

#### 2.4.1 工作流程

（待设计 — 核心问题：如何把导师的论文放在第2段的领域框架中标注位置？）

#### 2.4.2 技术原理与执行

（待设计 — 需确定：论文关系分类体系、时间线叙事构建方法）

#### 2.4.3 输出规格（03_论文定位.md）

（待设计 — 预期包含：论文时间线叙事、每篇在领域树上的位置标注、转型节点分析）

---

### 2.5 第4段：学习讲义设计（待填充）

#### 2.5.1 工作流程

（待设计 — 核心问题：如何从高数+线代反推到导师前沿的知识链？）

#### 2.5.2 技术原理与执行

（待设计 — 需确定：知识链反推策略、教材章节选择方法、递进路线排序算法）

#### 2.5.3 输出规格（04_学习讲义.md）

（待设计 — 预期包含：章节路径、每章知识点、推荐资源索引、"起承转合"叙事流）

---

### 2.6 跨段公共设计

#### 2.6.1 交付形态

工具不是网站/App，是**一套 AI 可读的指令文件 + 极简脚本**。

用户使用流程：

```
git clone 仓库 → 安装 Skill
/research-advisor "张鹏举" "中科院物理所" "https://..."
        ↓
Agent 先输出 00_身份验证卡.md → 用户确认 →
Agent 跑完整流水线 → 项目/张鹏举/ 下生成五份 .md
```

仓库结构：

```
仓库根/
├── CLAUDE.md
├── .claude/skills/research-advisor/
│   ├── SKILL.md                     # 流水线指令（核心）
│   └── 模板/                        # 五份输出骨架
│       ├── 00_身份验证卡.md
│       ├── 01_基础画像.md
│       ├── 02_领域脉络.md
│       ├── 03_论文定位.md
│       └── 04_学习讲义.md
├── 脚本/
│   ├── fetch_papers.py             # 调 OpenAlex 拿论文（可选）
│   └── build_index.py              # 扫 frontmatter 重建索引
└── 项目/                            # 产出目录
    ├── 导师/
    │   └── 张鹏举/
    │       ├── 00_身份验证卡.md
    │       ├── 01_基础画像.md
    │       ├── 02_领域脉络.md
    │       ├── 03_论文定位.md
    │       └── 04_学习讲义.md
    └── 索引/
        ├── 按机构.md
        └── 按方向.md
```

零服务器、零数据库、零部署。SKILL.md 是核心——一份 AI 能按步骤执行的流程文档。

#### 2.6.2 数据源

| 源 | 协议 | 免费额度 | 用途 |
|:---|:----|:--------:|:-----|
| OpenAlex | REST API（无 key） | ∞ | 论文/引用/机构/作者消歧 |
| Semantic Scholar | REST API（无 key） | 100 req/s | 论文影响力/领域分类 |
| arXiv API | REST 搜索 | ∞ | 最新预印本 |
| ORCID | REST API（无 key） | ∞ | 职业历史交叉验证 |
| 机构主页 | HTTP 抓取 | ∞ | 中文/机构特有信息 |
| 中文百科/新闻 | Web 搜索 | 取决于搜索工具 | 背景/采访/报道 |

#### 2.6.3 数据组织

每个老师一个独立目录（`项目/导师/<姓名>/`），全身只在这里存一份。`01_基础画像.md` 顶部用 YAML frontmatter 标注分类元数据：

```yaml
---
affiliation: 中科院物理所
department: 超快物质科学中心
tags: [超快光学, 强场物理, 阿秒科学]
---
```

`build_index.py` 扫描 `项目/导师/` 下所有 frontmatter，自动重建两个索引：

- **按机构.md**：学校→学院→部门→老师（树形）
- **按方向.md**：研究方向→老师（跨机构散点网络）

新增一个老师 → 脚本重跑 → 索引自动更新。不加新老师时不需要跑脚本。

#### 2.6.4 贡献与迭代模式

| 层 | 门槛 | 内容 |
|:---|:---:|:-----|
| 数据修正 | 极低 | PR 改 `.md` 文件里的论文标题/DOI/URL |
| 新增导师 | 低 | 跑一次流水线，核实产出，PR 合入 `项目/导师/<姓名>/` |
| 方法论改进 | 中 | 优化 SKILL.md / 模板结构 |

迭代循环：用户跑实例 → 发现问题 → 修正 → PR 合入 → 下一个人拿到修正版。PR 天然零冲突（每人独立目录）。"""

content = content[:start] + new_block + content[end:]

with open(filepath, 'w', encoding='utf-8') as f:
    f.write(content)

print('Block 2 replaced successfully')
print(f'New Block 2 length: {len(new_block)} chars')
"""

# Write script to file then execute
with open('v:/Default/Desktop/当前学习内容/寻找导师的邮件/李自翔/量子蒙特卡洛/pilot-test/temp_replace.py', 'w', encoding='utf-8') as f:
    f.write(script)

print('Script written')
