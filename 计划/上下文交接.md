# 上下文交接 — 新窗口快速启动

**版本**：v1.0（2026-06-30 第三轮）
**用途**：AI 读本文件 + 计划书.md 后应直接进入状态，不用读其他任何文件。

---

## 0. 一句话身份

pilot-test/ 是**导师研究方向调研工具**。学生输入姓名+机构+官网URL，AI 跑五份文档流水线（身份验证→基础画像→领域脉络→论文定位→学习讲义），产出到 `项目/导师/<姓名>/`。当前阶段 0/1 已实现，阶段 2/3/4 待启动。

---

## 1. 当前进度

| 组件 | 状态 |
|:-----|:-----|
| 计划书 `计划/计划书.md` | ✅ 第1章（需求）+ 附录（技术决策）已完成。第2章骨架完成，2.2内容分布无序需整理 |
| Config `config/sources.json` | ✅ 数据源 + 学科字典 |
| Script `scripts/discipline_classifier.py` | ✅ 已测，学科识别工作正常 |
| Script `scripts/identity_resolver.py` | ✅ 已写但 urllib 有 bug，未修复 |
| 其他 6 个脚本（profile_fetcher、web_discovery 等） | ❌ 未写 |
| Skill `research-advisor/SKILL.md` | ✅ 已写（路由+门控） |
| Skill `references/00-phase.md` | ✅ 已写（阶段 0/1 详细步骤） |
| Skill `assets/00_身份验证卡.md` + `01_基础画像.md` | ✅ 已写（模板） |

---

## 2. 新窗口第一步

```
1. 读 计划/计划书.md（重点：第2章 + 附录A + 附录B）
2. 读 .claude/skills/research-advisor/（4 个文件全读）
3. 和用户确认：先整理 2.2 节格式，还是先修 identity_resolver.py 的 bug
```

---

## 3. 第2章核心问题

2.2 节目前太短（每个子段 1-2 句），附录 A 反而放了全部细节。需要**把附录 A.4（阶段 0/1 设计）和 A.5（输出文件）吸收进 2.2 正文**，让 2.2 本身完整可读。附录只留验证来源和扩展信息。

具体改法：
- 2.2.1 工作流程：展开五步和四步，每步 1-2 句
- 2.2.2 技术原理：展开身份消歧原理（官网做 ground truth）、学科自动识别逻辑
- 2.2.3 输出规格：展开两份文件字段

---

## 4. 关键技术决策摘要（对话已确认）

- 阶段 0/1 合并一个 skill（profile-resolver），人闸串起来
- 脚本放项目根 `scripts/`，config 放 `config/`，skill 不含脚本
- 身份消歧用官网论文做 ground truth，不走纯 ML
- 学科感知决定 INSPIRE-HEP（高能）或 NASA ADS（天文）是否启用
- 不报错退出——全降级模式

## 5. 代码 bug（已确认停修）

`identity_resolver.py` 的 `urllib.parse.quote(v, safe="")` 在 Python 3.12 抛 `TypeError`。待用户确认后再修。

## 6. 废弃/不用的文件

所有过期文件已移入 `存档/`（00-需求草稿、01-session交接文档、06-旧学习历史、初衷、产出、指导等）。**新窗口不读存档/ 下任何文件**。

---

**版本**：v1.0
**生成日期**：2026-06-30
