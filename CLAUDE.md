# pilot-test CLAUDE.md

本文件只为 Claude Code 兼容保留。AGENTS.md 是当前项目规则的单一事实源。

新窗口或上下文压缩后，先读 `AGENTS.md`，再按 `QUICKSTART.md` 和 `docs/上下文交接.md` 恢复当前状态。涉及导师调研、`00-04` 成品或阶段 2-4 修订时，再读 `.claude/skills/research-advisor/SKILL.md`。

不要在 CLAUDE.md 里新增或复制项目规则；需要沉淀规则时按 `AGENTS.md` 的反臃肿原则放到对应唯一归属。
