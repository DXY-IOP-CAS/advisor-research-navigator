# 计划书

**版本**：v5.0（Harness 哲学 + 阶段 1 设计重写）
**生成日期**：2026-07-02
**说明**：原 `需求确认.md` 升格为 `计划书.md`。结构按"已确定 / 待设计"二分组织，避免上下文腐烂。
**来源**：原需求确认文档 + 本轮三窗口沉淀（技术方案 v2.0、Skill 架构讨论、阶段 0/1 设计讨论）

---

## 目录

- [文件规范](#文件规范)
- [第1章: 需求背景](#第1章-需求背景)
  - [1.1 起因 / 工具目标](#11-起因--工具目标)
  - [1.2 目标用户画像](#12-目标用户画像)
  - [1.3 工具差异化与定位](#13-工具差异化与定位)
- [第2章: 四阶段流水线设计](#第2章-四阶段流水线设计)
  - [2.1 整体工作流](#21-整体工作流)
  - [2.2 阶段 1：原理与策略](#22-阶段-1原理与策略)
  - [2.3 输出文档规范](#23-输出文档规范)
  - [2.4 阶段 2/3/4 设计原则](#24-阶段-234-设计原则)
- [第4章: 使用最佳实践](#第4章-使用最佳实践)
- [附录 A: 已沉淀的技术决策](#附录-a-已沉淀的技术决策)
- [附录 B: 待设计与开放问题](#附录-b-待设计与开放问题)
- [附录 C: 项目部署清单](#附录-c-项目部署清单)

---

## 文件规范

本计划书统一按以下规则组织。

**编号体系**：h2（##）用于章（第X章），h3（###）用于节（X.Y），h4（####）用于节内子段（X.Y.Z）。禁止 h5+。

**每节三段式**（仅工具设计章强制，h3 下三个 h4 子段）：
- 工作流程 — 做什么，自然语言描述
- 技术原理 — 方法、API、异常处理
- 输出规格 — 文件名、模板、格式

**h4 子段内只用正文段落**。不插入块引用、粗体伪标题、列表嵌套或其他制造新层级的格式。

**代码块限三类**：示例输出、API 请求/响应、模板/骨架。

**已沉淀 / 待设计二分**：所有"已确认"内容写进正文；所有"未决定 / 待研究"事项写进附录 B，不混进正文。

**目录只列到 h3**。

---

## 第1章: 需求背景

### 1.1 起因 / 工具目标

每年硕士/博士生选导师是刚需。每年研一 3-4 月学生面临报志愿和套磁，核心痛点在四个环节。官网信息粗糙：只有论文列表和标题，没有科普级解读。有论文看不懂：论文要求研究生以上知识，学生只有本科基础。资源散乱无索引：论文、教材、视频各在各处。调研易绕圈：不知道该读什么、按什么顺序。

工具用四段嵌套解决这些问题。每一段的输出为后一段提供认知上下文，范围逐段收窄，理解逐段加深。工具同时承担三个递进职能：调研（全面收集信息）、综述（分析串联知识）、教学（设计递进学习路线）。

| 段 | 做什么 | 做法（高层次） | 目的 |
|:---|:---|:---|:---|
| 第1段 | 老师基础信息调研 | 地毯式收集官网、学术平台、中文百科、采访稿等多维信息，多源交叉比对 | 输出基础画像，明确领域范围 |
| 第2段 | 领域发展调研 | 基于第1段的领域范围，追溯子领域发展脉络和当前阶段 | 输出认知地图，理解位置 |
| 第3段 | 论文串联与定位 | 以第2段认知框架重新审视论文序列 | 标注每篇论文在领域树上的位置 |
| 第4段 | 从零到前沿讲义 | 从高数+线代起步反向设计学习路线 | 输出递进式讲义（小字典） |

嵌套逻辑：大领域一>小领域一>老师定位一>学习路径。后一段建在前一段的认知之上。

技术实现细节在第2章和附录 A。

### 1.2 目标用户画像

工具的目标用户是大二/大三物理本科生，正在准备申请硕博选导师。核心场景是研一 3-4 月报志愿时，用工具判断与导师研究方向的匹配度。

工具假设用户从高数和线代起步。实际用户可能已有四大力学、固体物理等基础，但所有高阶知识（复变、数理方法、高量、群论、量子多体）本质上是高数和线代的逐步推广。从根推导才能零跳步建立真理解。

教材在 AI 时代不再是逐页通读的课本，而是字典。本科到研一的教材体系分三个层面：

| 层面 | 课程 | 定位 |
|:---|:---|:----|
| 数学基础 | 高数、线代、概率论、复变、数理方法 | 推导起点 |
| 物理基础 | 普物、四大力学、固体物理 | 物理直觉来源 |
| 高阶延伸 | 高量、群论、量子多体、量子场论、规范场论 | 大字典边界—领域触及的范围 |

教材从课本转向字典，有两个原因。第一，教材面向通用人才培养，覆盖完整体系，但具体方向只用其中部分知识点。逐页通读在无关内容上浪费大量时间。第二，学生可在 AI 辅助下按需读取字典条目，带着研究方向去学，既能深化具体知识点理解，又能强化整体框架认知。

第4段产出的讲义是个性化小字典。它从教材体系（大字典）中只提取第1-3段调研确认需要的知识点，串联成一条从高数+线代到前沿的逻辑链。小字典扎根于大字典，以教材体系为参照系，约束 AI 创作边界，防止偏离轨道。

一个可扩展的方向：工具可附带教用户如何配合 AI 递进学习。AI 的优势是能根据用户水平动态适配，但多数人不擅长用 AI 辅助学习。加入"如何用 AI 学"的引导可降低门槛。此方向未确定，作为后续讨论参考。

### 1.3 工具差异化与定位

18+ 同类工具调研后确认：市场无直接竞品。现有工具分三类，各占一个不重叠的位置。

| 类别 | 代表工具 | 做什么 | 与我们的重叠 |
|:-----|:---------|:-------|:-----------:|
| 导师评价 | 导师评价网、GradChoice、advisor-calculator | 口碑评分（人品、管理、毕业） | 不重叠——我们不碰评价 |
| 导师匹配 | PhdFit、StreamlinedAI、PhDuo | 从学生 CV 推荐导师 | 不重叠——我们不做推荐 |
| 学者画像 | supervisor、SciAtlas、ai-talent-graph | 论文+主页一>信息聚合报告 | 第1段部分重叠 |

学者画像工具停在"导师长什么样"的层面。我们继续往前推到领域发展、论文定位和教学讲义。

工具的准确定位包含四层含义。第一，通用模板引擎：对任何机构的任何老师适用，跨老师复用零边际成本，首个实例为 IPHY 超快物质科学中心某位导师。第二，开源方法论：Claude Code Skill + `src/phase1/` 脚本 + Markdown 模板形态，不是网站/App，零部署。第三，从调研走到教学：调研+综述+教学三职能递进。第四，调研驱动设计：每项决策经同类工具验证，不凭空发明。

一条根本差异区分了我们和所有竞品：同类工具帮学生找谁合适（匹配引擎）或知道老师什么样（画像工具）；我们帮学生学懂一个方向。

工具六不做：不写教材正文；不替代主动学习；不点评老师或推荐方向；不捏造论文/教材链接；不评价导师口碑；不搭建网站。

---

## 第2章: 四阶段流水线设计

**技术落地细节见** `src/phase1/pipeline.md`（CLI 命令、字段格式、API 参数）。本章只记录设计决策，不重复实现细节。

### 2.1 整体工作流

用户提供导师姓名 + 机构 + 官网 URL，工具执行四份文档流水线。每份文档的认知上下文来自前一份，深度逐段加深。早期需求曾按三层结构描述为“信息汇总 → 领域认知 → 学习讲义”；当前四份文档不是推翻三层结构，而是把“论文串联与定位”从领域认知和学习讲义之间独立出来，作为从导师事实走向学生学习路径的中间桥梁。

```
阶段1：基础画像 → 结构化画像 + 按学术履历分组的论文列表
    ↓
阶段2：领域脉络 → 当前申请方向的学科位置、发展脉络、关键问题
    ↓（认知回环，非单向）
阶段3：论文定位 → 用领域框架解释相关论文的研究路线和技术细节
    ↓
阶段4：学习讲义 → 从高数+线代到前沿的递进学习路径
```

阶段 1 的论文按学术履历阶段分组，不是为了按年份归档，而是为了观察导师在不同身份、单位和合作网络下研究方向如何微调或转向。学生申请导师时，最需要理解的是导师当前单位、当前课题组中最可能从事的研究领域；早期履历的价值在于解释当前方向从哪里来、哪些能力和问题意识被延续下来。

阶段 2 建立当前方向的领域认知，反过来让阶段 3 的论文串联更有深度。阶段 3 不是简单的时间线排列，而是在“老师当前做什么领域”的基础上讲清“哪些论文属于当前主线、哪些是前史积累、为什么这些技术路线会收敛到现在的课题”。阶段 4 再把前三段反向翻译成学生可执行的学习路径。

**阶段一览**：

| 阶段 | 输入 | 产出 | 核心任务 | 人闸 |
|:-----|:-----|:-----|:---------|:----:|
| 1 基础画像 | 姓名+机构+官网URL | `01_基础画像.md` | 官网提取身份+履历 → 三源论文采集 → 同名过滤 → `career_stages.json` 按履历切分论文 → 9 节画像 | 无（AI 自主 + verify 门控） |
| 2 领域脉络 | 画像+论文列表 | `02_领域脉络.md` | 快速回顾导师学术路径，重点解释当前申请方向在学科层级中的位置、发展脉络、关键问题和前沿 | TBD |
| 3 论文定位 | 领域框架+论文列表 | `03_论文定位.md` | 用阶段 2 的专业框架重读相关论文，区分当前主线、前史积累、旁支论文和需复核弱证据 | TBD |
| 4 学习讲义 | 前三段全部产出 | `04_学习讲义.md` | 从高数+线代和本科物理出发，反向设计进入当前方向前沿的递进学习路径 | TBD |

### 2.2 阶段 1：原理与策略

#### 2.2.1 三源互补设计

三个数据源各取所长，不追求任一源独立完整。

**GS 是论文列表主源，OpenAlex 做元数据补充，arXiv 做预印本时间差填补。** 选择 GS 做主源的判断来自一个根本事实：GS profile 由学者本人维护，OpenAlex 是机器聚类。对于任何学者，前者的论文完整性远高于后者。

| 对比维度 | Google Scholar | OpenAlex | arXiv |
|:---------|:---------------|:---------|:------|
| 论文列表来源 | 学者本人维护 | ML 算法聚类 | 作者自提交 |
| 对中文作者覆盖 | 完整（学者自己添加） | 不完整（22-38%） | 依学科而定 |
| 元数据（DOI/期刊/作者） | 无（只有标题+年份+引用数） | 完整 | 部分（DOI 可选） |
| 同名干扰 | 无（邮箱验证） | 高（需多级过滤） | 高（只靠 au: 文本匹配） |
| 限速风险 | CAPTCHA | 100K/天免费 | 3 秒/请求 |
| 可获得性 | GS ID 公开或可搜到 | OpenAlex ID 公开 | 全网公开 |

**采信优先级**：GS > OA > arXiv。多源重合论文以 GS 标题为准，OA 补充 DOI/期刊/作者，arXiv 补充预印本。

**GS 不可用时的降级**：如果找不到 GS profile 或被封锁，OpenAlex 做论文列表替补。AI 在画像开篇标注"未找到 GS profile，论文覆盖可能不完整"。三个数据源全部不可用则输出空列表。

#### 2.2.2 身份锁定（Phase A）

身份确认是整条流水线的基础。ID 错了，后面全是别人的数据。

**锁定协议**（四层递进，任一通过即停）：

- Tier 1（金标准）：官网 email 域名与 GS profile 的 verified email 域名匹配 → 身份锁定，GS profile 内所有论文属于此人，不执行同名过滤。这一层覆盖绝大多数教授。
- Tier 2（银标准）：ORCID 匹配。通过 OpenAlex 或官网获取 ORCID，与来源数据中的 ORCID 交叉验证。
- Tier 3（铜标准）：高度特定论文匹配。官网论文标题与 GS 论文标题精确匹配 3 篇以上 → 高度疑似同一人。
- Tier 4：全降级。以上都不通 → 跳过 GS，走 OpenAlex + arXiv。

**身份锁定完成后 AI 创建两个文件**：
- `verified_ids.json`：记录每个来源 ID、验证方式、通过哪一 Tier
- `career_stages.json`：从官网履历提取的结构化学术生涯记录

#### 2.2.3 学术阶段划分（core design decision）

**论文按履历分组的目的不是按年份归档，而是看每个职业阶段的研究方向演变。**

划分原则：**每个（时间区间 + 机构 + 职位）变化是一个独立阶段。** 机构没变但职位升了、方向变了，也要拆开。不能因为都在同一家单位就把多年揉成一团。

`career_stages.json` 同时承担三份职责：
1. 控制 §2 学术履历表的生成（脚本自动从 JSON 渲染表格）
2. 控制 §4 论文分组（脚本按年份区间把论文分配到对应阶段）
3. 给 AI 提供阶段叙事的锚点（每个阶段表格前写 1-2 句说明该阶段研究主题）

**它是学术履历的唯一事实源。** AI 写一份 JSON，脚本同时用它生成 §2 表格 + §4 分组。两条输出从同一份数据来，不可能不一致。旧方法（AI 手写 §2 表格 + 脚本默认 5 年分桶）已被放弃，因为两处数据会漂移。

#### 2.2.4 同名干扰过滤

过滤只在 GS profile 无法提供锚点（邮箱未验证或 GS 不存在）时触发。触发时使用三层网络评分，不依赖学科关键词（跨学科通用）：

- **合著者**：OA 独有论文与 GS 已确认论文共享合著者 → +2 分
- **期刊**：期刊名在已确认论文的期刊列表中 → +1 分
- **机构**：作者机构在已确认论文的机构列表中 → +1 分

总分 < 1 → 疑似同名干扰，过滤。≥ 1 → 保留。

arXiv 独有论文使用更简单的门控：有 DOI 的保留（可能是尚未被 GS 收录的预印本），无 DOI 的过滤（无法交叉验证）。

无 GS 锚点时的特例：整个 pipeline 只有 OpenAlex 数据。此时跳过过滤，全部保留，在画像中标注"未经合著者/期刊网络过滤，建议核查"。

#### 2.2.5 渲染与验证（Harness Loop）

渲染分两层：

1. **脚本生成骨架**（render_profile.py）：从 merged.json 读取导师信息和论文列表，从 career_stages.json 读取阶段配置 → 自动生成 §1 身份标识、§2 学术履历表、§4 论文分组表格、§8 数据质量统计。§3（研究方向）、§5（合作网络）、§6（公开信息）、§7（引用分析）、§9（验证来源）留占位符给 AI Edit。

2. **AI 填充叙事**：AI 通过 Edit 命令逐个替换占位符。不改论文表格行、不改格式，只写内容。

3. **verify 门控**（verify_profile.py）：最终质量检查，9 项全部通过才能声称完成。未通过时，AI 回到第 2 步修复，形成闭环：

```
渲染 → verify 检查 → 通过 → 完成
                    ↓ 未通过
                AI 看具体哪项失败 + 修复指引 → 修数据或改叙事 → 重新渲染 → 回到 verify
```

verify 的 9 项检查每项失败了会给出修复命令。Harness 不代替 AI 判断，但告诉 AI 往哪个方向修。

#### 2.2.6 arXiv 精确匹配流程

arXiv 的 `au:` 搜索基于字符串匹配，同名噪声率极高。优先使用更精确的方式：

1. **ORCID author identifier feed**（最精确）：如果作者已将 ORCID 关联到 arXiv 账号，访问 `https://arxiv.org/a/{ORCID}.atom2` 返回该作者全部论文（零噪声）。这是 opt-in 机制，不是所有作者都有。

2. **au: 搜索的优化**（前一步失败时使用）：将 `姓_名`（Wang_Shili）拆成 `au:Wang+AND+au:Shili` 匹配作者字段中同时出现姓和名的论文。加上 `-c` 学科分类过滤可进一步降低噪声。

3. **结果过滤**（render_profile.py 执行）：arXiv 独有论文无 DOI 的视为同名噪声过滤；有 DOI 的保留（可能是尚未被 GS 收录的预印本）。

**所有实现细节（CLI 命令、字段格式、API 参数、质量门清单）见 `src/phase1/pipeline.md`。**

### 2.3 输出文档规范

项目采用三层文档结构，各层不重复：

| 层 | 受众 | 文件 | 内容 |
|:---|:------|:-----|:-----|
| 设计决策 | 人 | `docs/计划书.md` | 为什么这么设计 |
| 技术参考 | 维护者 | `src/phase1/pipeline.md` | CLI、格式、API（单一事实源） |
| AI 指令 | Claude Code | `.claude/skills/research-advisor/` | 执行顺序、叙事规范、硬约束 |

**画像 9 节结构**（`01_基础画像.md`）：

| 节 | 内容 | 生成方式 |
|:---|:-----|:---------|
| §1 身份标识 | 姓名、机构、GS/OA/ORCID ID 等 | **脚本生成**（从 merged.json professor 字段） |
| §2 学术履历 | 时间-机构-职位-方向表格 | **脚本生成**（从 career_stages.json auto-render） |
| §3 研究方向 | 总述 + 具体方向 | AI Edit |
| §4 论文列表 | 按 career_stages 分组的论文表格 | **脚本生成**（6 列，含超链接） |
| §5 合作网络 | 高频合作者表格 | AI Edit |
| §6 公开信息 | 获奖、兼职、项目 | AI Edit |
| §7 引用影响力 | 总引用、h-index、近 3 年统计 | AI Edit |
| §8 数据质量 | 各源论文数、交叉验证统计 | **脚本生成**（从 merged.json statistics） |
| §9 验证来源 | 各数据源 URL + 验证状态 | AI Edit |

**画像格式规范**：
- 输出路径：`output/<学校>/<学院或研究所>/<部门>/<姓名>/01_基础画像.md`
- YAML frontmatter 含 affiliation、department、source_updated、orcid、google_scholar_url、openalex_id、run_timestamp、run_archive
- 缺失字段标 `[未找到]`，不捏造
- 不做导师评价（匹配度、推荐意见等禁止出现）
- 论文表格固定 6 列 #/年份/标题/期刊/引用/来源，**AI 不得修改表格行数或内容**

**原文中间产物**：
- 所有 step 输出、merged.json、career_stages.json 存入 `archive/<timestamp>/`
- Archive 不是缓存——每次运行从头查 API，archive 只供人工溯源

### 2.4 阶段 2/3/4 设计原则

#### 2.4.1 工作流程

阶段 2/3/4 的共同目标，是让学生从“知道一位导师的基本信息”过渡到“理解自己进组后最可能面对的当前研究领域”。三份文档不是并列报告，而是围绕同一条认知链逐步收敛：导师履历中的研究方向演化 → 当前申请方向的领域地图 → 相关论文在该地图上的位置 → 从本科基础进入该前沿的学习路径。

阶段 2（领域脉络）先用短段落回顾导师整个人生和学术路径，让学生建立人物认知和时间框架；随后迅速收敛到导师当前单位、当前课题组最可能开展的研究领域。早期方向只作为当前方向的前史和能力来源处理，除非它仍直接延续到当前课题组。阶段 2 要回答：这个导师现在所在的方向属于哪个一级/二级/更细子领域，领域在解决什么问题，关键技术路线是什么，导师当前工作处在什么位置。

阶段 3（论文定位）用阶段 2 的领域框架重新阅读论文列表。它不复述每篇论文摘要，而是区分四类论文：当前方向主线、当前方向的技术前身、早期履历背景、旁支或弱证据论文。阶段 3 要回答：哪些论文最能说明导师现在具体在做什么，研究路线如何形成，哪些技术细节会成为学生进组后的理解门槛。

阶段 4（学习讲义）把前三段反向翻译成学习路径。它不是通用课程表，也不是教材正文，而是从高数、线代和本科物理出发，抽取读懂当前方向所需的知识点，串联成个性化小字典。阶段 4 要回答：学生如何从已知基础逐步进入导师当前前沿，哪些概念必须补，哪些高阶主题只需按需查。

#### 2.4.2 技术原理

阶段 1 的按履历分组是阶段 2/3/4 的关键输入。导师身份、单位、职位和合作网络变化，常常对应研究对象、实验平台、技术路线或问题意识的微调；即使方向没有显著变化，分开观察也能避免把多年工作揉成一个静态标签。对申请学生而言，越接近当前单位和当前课题组的工作，通常越能代表进组后可能接触的领域；早期阶段的意义在于解释当前方向的来源，而不是平均展开全部历史方向。

阶段 2 的领域地图必须以“当前方向”为中心，而不是写成大百科。写作时从大到小定位学科层级：一级学科、二级方向、更小子领域、关键技术路线、当前前沿。阶段 3 只在这个地图上定位论文，不另起一个不相关的论文综述。阶段 4 只为这个地图和这些论文服务，不写通用本科到研究生课程大纲。

证据强度要贯穿三段。来自官网、Google Scholar、OpenAlex、ORCID、DOI 或论文页面的判断优先；单源论文、同名风险论文、来源缺失内容只能作为弱证据或标注“需人工复核”。来源缺失时写 `[未找到]`，不得为了补齐叙事捏造 URL。

#### 2.4.3 输出规格

`02_领域脉络.md` 的建议结构：运行信息；导师路径速览；当前方向学科定位；领域发展树；关键问题和技术路线；当前前沿；与导师当前课题组的关系；来源与待复核点。该文档可以提早期履历，但篇幅服务于解释当前方向，不平均介绍所有历史方向。

`03_论文定位.md` 的建议结构：运行信息；领域树节点定义；论文定位总表；当前主线论文详解；前史积累论文；阶段转折点；弱证据和旁支论文；给学习讲义的知识点清单。该文档允许进入技术细节，但每个技术细节都要服务于阶段 4 的学习路径。

`04_学习讲义.md` 的建议结构：运行信息；读者起点；学习主线总图；数学基础；物理基础；领域核心工具；导师当前方向专题；论文阅读顺序；资源指针；卡点提示。它应输出学习路径、概念解释和资源索引，不替代教材正文。

---

## 第3章: 验收与当前状态

[待设计]——本轮先把工具搭起来，验收标准待阶段 0/1 完整跑通后定。

---

## 第4章: 使用最佳实践

本章写给使用者（你自己）。**纠正两点：AI 不能替代 session 管理，工具不是万能胶。**

### 4.1 session 分割原则

每轮 session 只做一类工作。不要在同一轮里同时"设计文档 + 写代码 + 修 bug + 整理目录"——做混了就是我在这个窗口犯的错。

推荐分法：

| session | 任务 | 时长参考 |
|:--------|:-----|:--------|
| 设计 | 写计划书章节、定 workflow | 30-60 分钟 |
| 编码 | 写脚本、测试 | 60-90 分钟 |
| 整理 | 归档、改目录结构 | 15-30 分钟 |
| 调研 | 搜索资料、读工具文档 | 30-45 分钟 |

### 4.2 checkpoint 机制

每完成一个独立任务，写一段 checkpoint 到 `计划/上下文交接.md`。格式很简单：

```
2026-06-30 checkpoint: 跑通 identity_resolver 的 OpenAlex 查询（有 bug 未修）
```

写完后 `/clear` 再开下一个任务。

### 4.3 什么时候该停

- 纠正同一件事两次后 → 不继续，直接写 checkpoint 然后 `/clear`
- AI 开始自行加文档/重构/提议 → 停下，明确说"只做 X，别做 Y"
- 超过 45 分钟 → 该 session 已经够长了，收尾 or `/compact`

### 4.4 `/compact` 的正确用法

```
/compact 保留以下决策：A.1-A.7（数据源体系和架构形态），附录 C（部署清单），2.2.1（阶段 0/1 workflow）。丢弃：过往对话历史、旧代码输出、调试记录。
```

不要在自动 compact 触发后才处理。自动 compact 会丢关键早期决策。

---

## 附录 A: 已沉淀的技术决策

本附录收录本轮三窗口中已确定的技术决策。后续如有变更需走"用户拍板"流程。

### A.1 整体方向（已确认）

**A.1.1 信息源不是单一的**。早期讨论陷入"OpenAlex 是主源，其他补充"的死胡同。修正：物理学科按学科分层数据源优先级。

**A.1.2 身份消歧不走纯 ML**。OpenAlex 等 ML 聚类对中文名+跨机构不可靠（实测某中文姓名学者返回 25 个同名候选）。我们的方法：官网论文做 ground truth，论文标题直接比对。

**A.1.3 必须有人闸**。身份消歧不允许全自动。任何 ID 锁定前用户确认，避免连锁错误。

**A.1.4 不报错退出**。所有 API/数据源失败时降级到其他源，不抛异常。

### A.2 数据源体系（已确认）

按学科+ID 类型分层：

**跨学科通用 ID 体系**：
- ORCID（by email，注册过就有，最可靠）
- OpenAlex Author ID（ML 聚类，覆盖广但中文不准）
- Semantic Scholar Author ID（ML 聚类，覆盖广，TLDR 资源）
- ArXiv Author ID（用户主动 claim 后生成，物理/数学/CS 常见）
- Google Scholar Profile（用户自建，无官方 API）
- CrossRef

**物理学科专用**：
- **INSPIRE-HEP** — 高能物理/粒子物理/核物理领域事实标准，API 全
- **NASA ADS** — 天体物理事实标准，需 API token

**网页抓取层**：
- 机构官网（HTTP fetch）
- Web Search（Serper/Exa/Tavily）
- 百度百科/知乎/B站（Web Search）

**不引入**：Scopus、Web of Science（付费）；CNKI/万方（需授权）；DBLP（CS 专用）；Neo4j（重）。

详见 `config/sources.json`（项目级共享配置）。

### A.3 架构形态（已确认）

工具不是"AI 在空白 Claude Code 里按文档操作"，而是：

```
pilot-test/
├── .claude/skills/research-advisor/   # Claude Code Skill（指令 + 模板）
│   ├── SKILL.md                       # 入口，渐进式披露
│   ├── assets/                        # 输出模板
│   └── references/                    # 按需加载的细节
├── config/
│   ├── sources.json                   # 数据源 + 学科字典
│   └── disciplines.json               # [TBD] 学科→关键词独立文件 [可选]
├── src/phase1/                        # 阶段 1 Python 脚本
│   ├── pipeline.md                    # 技术实现文档（单一事实源）
│   ├── run.py                         # 统一入口（快捷方式，非必须）
│   ├── step1_discipline.py            # 学科关键词→arXiv 分类
│   ├── step2_gs.py                    # scholarly 封装
│   ├── step3_openalex.py              # OpenAlex API
│   ├── step4_arxiv_id.py              # arXiv author identifier 精确匹配
│   ├── step5_arxiv.py                 # arXiv au: 搜索（回退方案）
│   ├── step6_merge.py                 # 多源合并去重
│   ├── render_profile.py              # 骨架生成
│   ├── verify_profile.py              # 质量门控
│   ├── validate_career_stages.py      # career_stages 自检（轻量，仅警告）
│   └── utils.py                       # 共享工具 + 噪声过滤
├── output/<机构>/<部门>/<姓名>/       # 每位导师的产出
│   ├── 01_基础画像.md
│   └── archive/<timestamp>/          # 中间产物存档
├── archive/                           # 旧版脚本存档
└── docs/                              # 规划与调研文档
```

**渐进式披露原则**：SKILL.md ≤500 行，只含路由 + 当前阶段入口。详细步骤进 `references/<phase>.md`。本原则来自 Trail of Bits `designing-workflow-skills`。

**Skill 不内嵌 scripts/config**——脚本和配置归项目根，SKILL.md 用相对路径引用。这是按 CLAUDE.md "隔离边界" 原则。

### A.4 学科 → 数据源映射（已确认）

| 学科 | 关键词（部分） | 启用数据源 | ArXiv 分类 |
|:-----|:--------------|:----------|:-----------|
| 高能物理 | HEP, quark, higgs, ... | INSPIRE-HEP + arXiv + OpenAlex | hep-ph, hep-th, hep-ex, nucl-* |
| 天体物理 | astrophysics, cosmology, ... | NASA ADS + arXiv + OpenAlex | astro-ph |
| 凝聚态 | superconduct, topological, ... | OpenAlex + arXiv | cond-mat.* |
| 原子分子光 | attosecond, HHG, strong field, 阿秒, 强场, ... | OpenAlex + arXiv | physics.atom-ph, physics.optics |
| 量子信息 | qubit, quantum, 量子信息, ... | arXiv + OpenAlex | quant-ph |
| 通用物理 | physics | OpenAlex + arXiv + S2 | physics |

完整字典见 `config/sources.json` 的 `disciplines` 段。

### A.5 验证来源

**OpenAlex 评估**：
- 官方 Name Disambiguation（XGBoost）：https://github.com/ourresearch/openalex-name-disambiguation
- Zhao & Chen (2025) 消歧精度评估：https://arxiv.org/html/2502.11610v2
- Zheng et al. (2025) 中国论文覆盖：https://doi.org/10.1002/asi.70013
- Céspedes et al. (2025) 语言元数据：https://doi.org/10.48550/arxiv.2409.10633

**API 文档**：
- arXiv API：https://info.arxiv.org/help/api
- arXiv Author Identifiers：https://info.arxiv.org/help/author_identifiers.html
- OpenAlex API：https://docs.openalex.org
- ORCID Public API：https://info.orcid.org/documentation/api-tutorials
- INSPIRE-HEP API：https://github.com/inspirehep/rest-api-doc

**学术消歧参考**：
- WhoIsWho（清华）：https://github.com/THUDM/WhoIsWho
- CrossND（人大）：https://github.com/zfjsail/CrossND

**Agent / Harness 工程**：
- Harness Engineering（OpenAI）：https://openai.com/index/harness-engineering/
- Agentic Harness Engineering：https://arxiv.org/html/2604.25850v1
- Loop Engineering（Addy Osmani）：https://addyo.substack.com/p/loop-engineering
- Claude Code Skills 规范：https://code.claude.com/docs/en/skills
- Trail of Bits Workflow Skills：https://github.com/trailofbits/skills

### A.6 架构形态 v2：Harness 设计哲学（已确认）

**Agent = Model + Harness**（Trivedy 2026）。本项目是 Harness，不是 Application。

传统软件工程试图在代码里覆盖所有边界情况，导致臃肿、难以适应突发状况。Harness 方式相反：把"智能"留给 AI Agent，把"结构"留给工具和规则。

| 维度 | Application 方式（不采用） | Harness 方式（采用） |
|:-----|:--------------------------|:-------------------|
| 错误处理 | 代码里 try/except 全覆盖 | 失败快、报错明、交给 AI 修复 |
| 编排逻辑 | 主流程脚本串联一切 | 每个 CLI 独立，AI 决定顺序和重试 |
| 校验 | 前置校验（gate）阻止错误 | 后置校验（verify）暴露错误供修复 |
| 状态 | 内存或 DB | JSON 文件（可观察、可恢复、可检查） |
| 扩展 | 加代码、改架构 | 加脚本、改配置 |

**Harness 三要素**：
1. **工具**（tools）：每个 step 是独立 CLI，入出都是 JSON。run.py 只是快捷方式，AI 可跳过它单独调用任何 step。
2. **规则**（rules）：CLAUDE.md、pipeline.md、phase1.md 定义"怎么做"和"不能做什么"。
3. **评价标准**（eval）：verify_profile.py 是最终质量门控。未通过 → AI 修 → 重渲染 → 再验。

**Loop 工作原理**：
```
Phase A → Phase B → C（渲染）→ verify 通过？→ 完成
                              ↓ 未通过
                      AI 诊断 → 修复 → 再渲染 → 回到 verify
```

**不做什么**（来自实践教训）：
- validate_career_stages.py 不做强门控（只警告，不阻止）。verify 才是门控。
- run.py 不做全能编排（出错就报，不自己处理降级）。AI 看到错误自行决定下一步。
- 不缓存中间结果（每次从头查 API，archive/ 只供人工溯源）。
- 不给每个脚本加 schema 校验（校验成本超过犯错成本，AI 检查输出更快）。

**为什么必须这样做**：产出是 Markdown 文档，不是软件。文档的质量标准（内容准确、格式规范、来源可溯）靠 AI 凭判断力保证，不是代码能硬编码的。代码只保证数据获取正确、格式不被破坏。两个层面分离，才能各自演进。

---

## 附录 B: 待设计与开放问题

本附录追踪所有"未决定 / 待研究 / 搁置"的事项。每条都有归属——后续拍板时回到本附录划掉或移到附录 A。

### B.1 阶段 1 待细化

- [ ] 无 GS 锚点时 OA 污染自动标记——当前只能靠 AI 人工核查论文标题是否属于该研究方向。可考虑基于 arXiv 分类做自动过滤

### B.2 Skill 设计待定

- [ ] `references/02-04-phase.md`（阶段 2/3/4 详细步骤，**等阶段 1 跑通再写**）

### B.3 阶段 2/3/4 [全部待设计]

本轮约定：阶段 1 跑通后立即开始阶段 2 设计。不提前搭骨架，避免空设计干扰。

### B.4 工程级问题

- [ ] OpenAlex API key 是否设为可配置（当前无 key，polite pool 限速 10 req/s）
- [ ] 索引文件（按机构 + 按方向）何时引入

### B.5 跨学科扩展 [搁置]

- [ ] DBLP 在 CS 领域如何适配
- [ ] PubMed 在生物医学领域如何适配
- [ ] CNKI / 万方在中文社科领域如何适配（需授权问题）

---

## 附录 C: 项目部署清单

工具运行所需的非-Skill 基础设施：

### C.1 环境要求

- Python 3.12+
- 操作系统：Windows 11 / Linux / macOS（跨平台）
- 网络：可访问各 API（OpenAlex、S2、ORCID、arXiv、INSPIRE-HEP）

### C.2 配置文件

| 文件 | 用途 | 状态 |
|:-----|:-----|:-----|
| `config/sources.json` | 数据源 + 学科字典 | v1.0 已在 |
| `config/disciplines.json` | [可选] 独立学科字典 | 未决 |
| ADS_API_TOKEN（环境变量）| NASA ADS 认证（如启用天体物理）| 按需 |

### C.3 脚本清单

所有阶段 1 脚本集中在 `src/phase1/`，每步独立通过 JSON 通信。完整清单见附录 A.3 的文件树。

**四个值得注意的脚本**：
- `run.py` — 统一入口，快捷方式。AI 也可跳过它手动调每个 step
- `render_profile.py` — 骨架生成 + §2 自动渲染 + §4 论文分组，**不负责叙事内容**
- `verify_profile.py` — 最终质量门控，9 项检查 + 修复指引。**所有产出必须通过 verify 才能声称完成**
- `validate_career_stages.py` — career_stages.json 格式自检（轻量，仅警告）
- `validate_verified_ids.py` — verified_ids.json 格式自检
- `ProfDirResolver`（utils.py 类）— 从 prof_dir 自动解析所有路径

### C.4 Skill 清单

| 文件 | 用途 | 状态 |
|:-----|:-----|:------|
| `.claude/skills/research-advisor/SKILL.md` | 总入口 | ✅ 已写 |
| `.claude/skills/research-advisor/references/phase1.md` | AI 叙事规范与硬约束 | ✅ 已写 |
| `.claude/skills/research-advisor/references/01-data-sources.md` | API 调用细节 | ✅ 已写 |

### C.5 数据流向（阶段 1）

```
用户输入 (姓名/机构/官网URL)
       │
       ▼
Phase A：AI 广域搜索 + 身份确认
  MCP Fetch 读官网 → 提取 email/名称/研究方向、履历
  MCP 搜 GS/ORCID/OpenAlex 链接 → 交叉验证
  → 产出存 archive/<ts>/：
    ├── 00_verified_ids.json（身份验证记录）
    └── career_stages.json（学术阶段配置，每一职位变化独立一段）
       │
       ▼
Phase B：脚本数据获取（--archive-dir 自动推导路径）
  run.py --university 大学 --institute 研究所 --name 姓名 ...（结构化参数）
  或手动：step2_gs --archive-dir ...（自动输出到 archive/<ts>/01_gs.json）
   │
   ├── step2_gs       → archive/<ts>/01_gs.json
   ├── step3_openalex → archive/<ts>/02_oa.json
   ├── step4_arxiv_id → archive/<ts>/03_arxiv.json（ORCID 精确匹配）
   │   └── 失败 → step5_arxiv（姓名拼音 + 分类 → au: 搜索）
   ├── step6_merge    → archive/<ts>/04_merged.json（三源合并去重）
   └── render_profile --archive-dir → prof根目录/01_基础画像.md
       │
       ▼
Phase C：渲染 + 循环验证
  render_profile --archive-dir → 自动找 career_stages.json、merged.json
  §2 学术履历（从 career_stages 自动生成）
  §4 论文分组（按 career_stages 年份切分）
  其余节占位 → AI Edit
       │
       ▼
  verify_profile --archive-dir → 自动找 merged.json
   ├── 通过 → ✅ 完成
   └── 未通过 → AI 看修复指引 → 修复 → 重新渲染 → 再验
```

---

**版本**：v5.0
**生成日期**：2026-07-02
**主要修订**：第 2 章全面重写——三源互补设计、身份锁定四层协议、career_stages 单一事实源、同名网络过滤、Harness Loop 渲染验证。附录 A.3 更新文件树。A.5 精简验证来源。A.6 新增 Harness 设计哲学。附录 B 清理失效条目。附录 C 简化脚本清单。
**下次更新**：阶段 2 启动后更新 2.4 节。
