# 上下文交接 — 新窗口任务：批量跑中科院物理所超快科学中心导师画像

## 一句话

用户给了 15 位 IPHY 老师的姓名+官网 URL（`docs/测试指令.md`）。逐个跑阶段 1 流水线产出 `01_基础画像.md`。

## 当前状态

| 已完成 | 需新窗口做 |
|:-------|:-----------|
| 张鹏举 ✅（58 篇，verify 10/10） | 其余 14 位老师 |
| 常国庆 ⚠️（204 篇，无 GS profile，OA 有污染） | 重跑常国庆（如果介意污染） |
| pipeline 代码已定型（11 脚本 + merge_tables 两步法） | 只需顺序执行，不改代码 |

## 项目结构速览

```
pilot-test/
├── src/phase1/              # 11 个脚本，1781 行
│   ├── step2_gs.py          # scholarly 取 GS（主源）
│   ├── step3_openalex.py    # OpenAlex 元数据
│   ├── step5_arxiv.py       # arXiv 预印本
│   ├── step6_merge.py       # 三源合并去重
│   ├── render_profile.py    # 渲染画像表格（脚本）
│   ├── merge_tables.py      # 两步法：AI 叙事 + 脚本表格合并
│   ├── verify_profile.py    # 成品 10 项质量门控
│   └── archive_previous.py  # 运行前自动存档旧版
├── .claude/
│   ├── rules/               # pipeline 执行规则
│   │   ├── 00-overview.md   # 三阶段总览
│   │   ├── 01-data-pipeline.md  # CLI + 数据格式
│   │   ├── 02-quality-gates.md  # 质量门
│   │   └── 03-archive.md    # 输出 + 存档规则
│   └── skills/              # 6个 superpowers 技能
├── output/                  # 已有张鹏举+常国庆
└── docs/测试指令.md          # 15 位老师名单
```

## 流水线（新窗口按此执行）

```
每个老师：
  Step 1: python src/phase1/archive_previous.py "<路径>/<姓名>"
          （自动存档旧版，output/ 没有该老师时跳过）

  Step 2: 阶段 A — AI 读官网 + MCP 搜 GS/ORCID → 邮箱验证
  Step 3: 阶段 B — 依次跑 step2_gs / step3_openalex / step5_arxiv / step6_merge
  Step 4: 阶段 C — render_profile → AI 润色（Write） → merge_tables → verify_profile

  verify_profile 全部 10 项通过后才能声称该老师完成。
```

## 输出路径

`output/中国科学院大学/中科院物理所/超快物质科学中心/<姓名>/`

## 已知问题（不阻塞，但需注意）

1. **常国庆无 GS profile** → OA 是唯一源。OA 的 h-index/引用数可能不准，画像中已标注
2. **OA 论文污染**（同名不同人）→ 已有 `is_oa_pollution` 过滤词（biology + condensed matter），但关键词匹配不完美。GS 存在的老师不受影响（GS 是主源）
3. **arXiv 同名噪声** → merge 步骤自动通过 DOI/标题匹配过滤
4. **不要并行跑 Subagent** → API 会超时。逐个串行跑
5. **不要改代码** → 如果遇到问题，记录到报告即可

## 这个窗口做过的事（新窗口不需要知道）

- schema.py 创建又删了、errors.py 创建又删了、66 个测试删到 0
- render_profile 的 ts_dir bug、邮箱双 @ bug、路径编码 bug
- superpowers 技能是从 obra/superpowers 复制了 6 个核心 skill
- 这些都已经修好了，新窗口不需要关心

## 启动指令（新窗口）

```
1. 读 CLAUDE.md + QUICKSTART.md
2. 读 .claude/rules/ 下 4 个文件
3. 读 docs/测试指令.md 获取 15 位老师名单
4. 从"已完成"以外的老师开始，逐个串行执行
```
