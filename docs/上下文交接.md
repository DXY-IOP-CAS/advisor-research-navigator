# pilot-test 上下文交接 - 2026-07-04

## 当前目标

当前分支：

```text
codex/phase4-learning-guide-redesign
```

当前最高优先级仍是把张鹏举材料打磨到可交给用户人工审阅的质量；第二优先级是把可复用规则沉淀到 `research-advisor` harness；第三优先级才是用同中心其他导师做泛化测试。

用户明确不喜欢外显分类框架，例如“语言层/图像层/方法层”“主线一/主线二”“第一类/第二类”“第一段路/第二段路”、训练营、打卡表或固定天数路线。当前文风方向是专家带读式的问题链：先讲论文或领域在推进什么问题，再把对象、方法、机制、图像和平台作为读论文时的定位问题。

2026-07-04 晚间收尾状态：张鹏举仍是主产品；汪非凡只做了一个受限 Phase 1 泛化试跑，用来检查阶段一工具链在非张鹏举导师上的鲁棒性，不是完整阶段 2-4 作品。

## 硬约束

- 不读取或引用 `archive/`，包括 `output/**/archive/**`。存档只供人工溯源。
- 不做导师评价，不写匹配度、推荐申请、值得报考、是否适合申请等判断。
- 来源必须能追溯到 URL；缺失写 `[未找到]`，合理但证据不足写 `需人工复核`。
- 每次运行阶段流程都从头查 API 和来源，不使用缓存或历史输出当证据。
- 阶段 2-4 写作、修订或质量判断必须先使用 `.claude/skills/research-advisor/`。
- verifier 只做结构、来源、禁用语、引用格式和 DOI 元数据 smoke，不等于学术质量通过。
- `risk_gate.py --prof-dir` 当前会读取 active archive 数据；在 archive 禁读约束下不要作为普通收口命令运行。

## Active 路径

```text
output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举/
```

当前 active 材料：

```text
00_材料导读.md
01_基础画像.md
02_领域地图.md
03_论文路线.md
04_学习向导.md
```

同中心批量清单：

```text
docs/batches/超快物质科学中心导师清单.md
```

该清单只记录官方导师列表和详情页 URL。只有张鹏举材料确认足够强之后，才用它做批量泛化测试。

## 当前材料状态

`00_材料导读.md` 已作为读者入口：先读领域地图、论文路线、学习向导，再回到基础画像核查事实。导读还给出液体水 ICD 起步闭环和一页讨论稿入口，并有自己的文末来源表。

`01_基础画像.md` 已从标题直接开始，不再暴露 YAML/frontmatter、`run_timestamp`、内部 archive 路径或隐藏 HTML 注释。当前身份主 ORCID 为 `0000-0001-7746-2113`；SF6 论文 DOI `10.34133/ultrafastscience.0147` 的 Crossref authenticated ORCID 与主体 ORCID 不一致，已写为单篇 DOI 元数据差异/可能重复 ORCID，需人工复核，但不改变主身份锁定。

`02_领域地图.md` 已从“领域发展树/箭头图/关键问题一二三”改成连续领域叙述：静态谱学 -> 碰撞动力学和多体弛豫 -> 飞秒超快谱学 -> XUV/阿秒科学 -> 液相和凝聚相复杂体系。近期还清掉了“分成几块”“竞争框架”等可见分类残留，并把“若干问题入口”的表述从显式数量改成更自然的连续问题入口。

`03_论文路线.md` 已从论文总表和编号主线改成问题链叙述。当前主要阅读动作是：先把液体中的电子信号站稳，再进入液体水 ICD 和电子-核运动竞争，随后看分子电子运动的时间和相位，最后把机制论文接回平台边界。近期还清掉了“属于哪个分类”“五个判断”“第一步/第二步/第三步”“摆进若干格子”“信号、时间、符合、平台四入口”等残留；“必备基础知识”表也改成由能量/信号读法、谱图到过程、电子相关和平台链路逐步接上的 prose。

`04_学习向导.md` 已从固定天数、训练营、外显分层和“第一段路/第二段路”标题，改成自然阅读动作标题：

```text
先知道光电子谱到底在看什么
再把静态谱读成时间过程
随后进入阿秒电子运动
进入液相和凝聚相复杂环境
最后把机制论文接回平台边界
```

起步闭环默认使用 2025 年液体水 ICD 竞争机制论文，回看 2022 年液体水 ICD 首次观测论文。读图链为 2025 Fig. 1(b,d) -> Fig. 2a-e -> Fig. 3/4/6 -> 2022 Fig. 3；平台链为 HHG/XUV 光源、液体微射流、电子-电子符合谱和机制判断。

`04_学习向导.md` 的终点能力段落已从“包括四件事”的清单口吻改成连续能力描述：先形成当前方向轮廓，再读懂主线论文入口，随后定位自身缺口，最后把阅读转成可交流的问题。进组后请教问题也从“拆成三个问题”的清单口吻改成论文、平台、机制之间的连续追问。

## Harness 状态

主要规则入口：

```text
.claude/skills/research-advisor/SKILL.md
.claude/skills/research-advisor/references/phase2-field-map.md
.claude/skills/research-advisor/references/phase3-paper-position.md
.claude/skills/research-advisor/references/phase4-learning-guide.md
.claude/skills/research-advisor/references/evidence-rules.md
.claude/skills/research-advisor/references/quality-gates.md
```

`verify_phase_docs.py` 现在会检查必需章节、引用键和来源表、正文裸 URL、参考文献章节位置、导师评价语、旧工程口吻、旧分类标题、固定天数路线、训练营口吻、聊天式二人称、`最小闭环` 旧术语、`最小可交付` 项目口吻、`证明学生` 过度承诺，以及“第几段路”编号式学习路径标题。

`04_学习向导.md` 模板已同步使用自然阅读动作标题。`phase4-learning-guide.md` 和 `docs/计划书.md` 都已说明设计理由：阶段四要像由浅入深的专家带读，不展示内部分类学。

旧过程文档 `docs/phase2-4_skill化设计草案.md` 已删除。设计原因看 `docs/计划书.md`；执行细节看 `references/`；可机械判断的门控看 `scripts/` 和测试。

汪非凡 Phase 1 试跑暴露了一个可复用工具链问题：Google Scholar-only 论文此前没有 DOI/arXiv 链接，导致 `01_基础画像.md` 论文来源无法通过阶段一 verifier。已在 `src/phase1/step2_gs.py` 保留 Google Scholar publication URL，在 `src/phase1/step6_merge.py` 透传，在 `src/phase1/utils.py` 作为论文链接 fallback，并让 `src/phase1/verify_profile.py` 接受 DOI、arXiv 或 Google Scholar paper link。对应测试在 `tests/test_phase1_harness.py`。

汪非凡当前仅有未跟踪 active `01_基础画像.md` 和 `latest.txt`，目录内还有 archive 子目录。除非用户明确要求继续该导师，否则不要提交这份试跑产物；即便继续，也只处理 active 文件，不读取或引用 archive。

## 最近提交

```text
b2e6a92 fix: verify google scholar paper links
4b42ddb fix: link google scholar only papers
5105c30 docs: refresh handoff after list wording cleanup
8cb9da0 docs: remove residual list-like Zhang wording
1fafb9d docs: refresh handoff after prose smoothing
0f302dd docs: smooth Zhang progression prose
d8348ed docs: compact handoff context
e4ac17b docs: record phase4 natural heading rule
259f308 docs: refresh handoff after cleanup audit
355589c docs: remove superseded phase2-4 draft
```

更多历史细节用 `git log --oneline` 和对应 diff 查，不要把旧聊天历史或旧交接全文重新塞回上下文。

## 最近验证

最近一轮已通过：

```bash
python -m unittest discover -s tests -p "test_*.py" -v
python -m unittest discover -s .claude\skills\research-advisor\tests -p "test_*.py" -v
python src\phase1\verify_profile.py --prof-dir "output\中国科学院大学\中科院物理研究所\超快物质科学中心\张鹏举"
python .claude\skills\research-advisor\scripts\verify_phase_docs.py --prof-dir "output\中国科学院大学\中科院物理研究所\超快物质科学中心\张鹏举"
python .claude\skills\research-advisor\scripts\verify_source_metadata.py --prof-dir "output\中国科学院大学\中科院物理研究所\超快物质科学中心\张鹏举"
python src\phase1\verify_profile.py --prof-dir "output\中国科学院大学\中科院物理研究所\超快物质科学中心\汪非凡"
git diff --check -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**'
```

结果：

- 根目录 unittest：37 个通过。
- research-advisor unittest：29 个通过。
- 张鹏举 `verify_profile.py`：OK。
- 张鹏举 `verify_phase_docs.py`：OK。
- `verify_source_metadata.py`：`[OK] DOI source metadata checks passed`。
- 汪非凡 `verify_profile.py`：OK。
- `git diff --check`：OK。

## 当前 git 状态

排除 archive 后，业务工作区剩余未跟踪的本地 `.codex/` 目录，以及汪非凡 Phase 1 试跑 active 文件。不要纳入提交，除非用户明确要求管理 Codex 本地配置或继续汪非凡试跑。若 `git status` 直接显示 `output/**/archive/**` 删除或变动，把它当作 archive 范围脏状态处理：不要读取、恢复或提交。

推荐检查：

```bash
git status --short -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**'
```

## 下一步建议

1. 如果用户回来要求解释进展，先从最近三类变化讲：张鹏举 `02/03/04` 去分类化、Phase 4 自然标题规则、旧草案去重。
2. 如果继续打磨张鹏举材料，优先人工审阅 `00_材料导读.md`、`03_论文路线.md`、`04_学习向导.md` 的读者路径是否顺，避免再新增长期 spec。
3. 如果要做泛化测试，使用 `docs/batches/超快物质科学中心导师清单.md` 选 1 位导师，从官方详情页和 Phase 1 初始化开始，从头 fresh search，不复用张鹏举数据。
4. 汪非凡可以作为下一轮 Phase 1 工具链回归样本，但目前不要把它当作完整交付材料；是否继续到阶段 2-4 需要重新检索和质量判断。
5. 批量测试前不要读取 `archive/`；不要跑会触碰 active archive 的命令。
6. 任何新长期规则只在满足“稳定提高四份文档质量、可跨导师复用、归属明确”时沉淀：设计原因进 `docs/计划书.md`，执行细节进 `references/`，确定性检查进 `scripts/`。

## 新窗口第一动作

1. 读本文件。
2. 读用户注入的 `AGENTS.md` 约束。
3. 读 `.claude/skills/research-advisor/SKILL.md`。
4. 按任务只读必要 references：阶段二读 `phase2-field-map.md`、`evidence-rules.md`、`quality-gates.md`；阶段三读 `phase3-paper-position.md`；阶段四读 `phase4-learning-guide.md`。
5. 不读 `archive/`。
