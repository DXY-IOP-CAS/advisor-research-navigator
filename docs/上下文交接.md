# pilot-test 上下文交接 - 2026-07-05

本文件只记录当前状态、风险和下一步。历史细节用 `git log`、提交 diff 和 `docs/e2e/` 回归记录追溯；不要把这里重新写成长流水账。

## 当前分支和目标

当前分支：

```text
codex/e2e-quality-redesign
```

当前目标：从第一性原理重构导师调研项目，使它能从最小输入 `姓名 + 机构路径 + 官网 URL` 端到端产出质量过关、可读、可审查、可批量复刻的 `00-04` 五份导师文档。

主路径：

```text
最小输入 -> Fact Pack -> Cognitive Blueprint -> 00-04 成品 -> 证据/图示/自审 -> 回归与瘦身
```

优先级：成品质量和教学质量优先，其次效率；项目瘦身优先于继续堆规则。旧 phase、旧脚本、旧模板、旧 verifier 只有在服务最终质量、可审查性或批量稳定性时才保留。

## 当前稳定入口

新窗口或上下文压缩后按这个顺序恢复：

1. 读 `AGENTS.md` 和 `QUICKSTART.md`。
2. 读本文件，确认当前分支、样本状态和下一步。
3. 涉及导师调研、00-04 成品或阶段 2-4 修订时，读 `.claude/skills/research-advisor/SKILL.md`。
4. 只有执行阶段 1 技术步骤时，再读 `src/phase1/pipeline.md` 和必要 references。
5. 运行 `git status --short -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**'`，确认只处理相关文件。

不要从旧 e2e 记录、旧 quickstart 或已删除的兼容脚本反推当前 workflow。当前 Phase 1 入口只认 `phase1_init.py + --prof-dir` 的逐步执行路径。

`docs/e2e/` 只作历史回归记录，`docs/batches/` 只作官方输入清单；二者都不是执行流程说明。

## 硬约束

- 不读取或引用 `archive/`，包括 `output/**/archive/**`。存档只供人工溯源。
- archive 禁读指 Agent 不得手动打开、搜索、复制或引用历史/中间文件；脚本通过 `--prof-dir` / `ProfDirResolver` 读取当前 active `_internal/archive/<ts>` 不属于手动读取 archive。
- 不提交 `.codex/` 或未明确纳入项目的本地目录，例如 `精髓转移/`。
- 导师成品根目录只能暴露 `00_材料导读.md`、`01_基础画像.md`、`02_领域地图.md`、`03_论文路线.md`、`04_学习向导.md` 和 `_internal/`。
- 来源必须可追溯；缺失写 `[未找到]`；弱推断写 `需人工复核`。
- 不做导师评价，不写匹配度、推荐申请、是否值得报考等判断。
- 每个里程碑只解决一类问题：成品质量、证据可靠性、教学设计、可视化、端到端自动化、项目瘦身或 verifier 底线。
- verifier 只做 smoke，不冒充学术质量。学术质量来自证据、蓝图、自审、回归和人工反馈。

## 已稳定的 harness 设计

### Fact Pack

阶段 1 仍是有价值的事实层资产：`phase1_init.py`、Google Scholar / OpenAlex / arXiv 采集、`risk_gate.py`、受控身份修正、受控论文剔除、`render_profile.py` 和 `verify_profile.py`。

新导师从三行输入初始化：

```bash
python src/phase1/phase1_init.py \
  --university "<大学>" \
  --institute "<学院或研究所>" \
  --department "<部门>" \
  --name "<中文名>" \
  --official-url "<导师官网 URL>"
```

后续统一使用脚本输出的 `prof_dir` 和 `--prof-dir` 参数。不要手动拼 `_internal/archive/<ts>`。

### Cognitive Blueprint

每位导师在写 `02-04` 前必须维护 `_internal/blueprint.md`。蓝图不是过程日志，而是认知设计合同，至少覆盖：

- 读者起点
- 导师当前方向一句话
- 目标论文及选择理由
- 核心图或图组
- 平台链路
- 课程到论文的学习桥
- 可视化计划
- 证据风险和人工复核

蓝图薄弱时先继续检索和核证，不要继续润色正文。成品正文必须消费蓝图，而不是只让 verifier 看到蓝图字段存在。

### 成品质量底线

每份成品文档都按“小白执行 AI + 小白学生读者”设计，必须自包含：说明本文解决什么问题、为什么这样读、读完应获得什么能力；关键术语和缩写首次出现时展开；不能假设读者读过项目规则、聊天记录或其他导师样本。

可视化是理解工具，不是装饰。不要把所有内容画成 Mermaid；领域定位优先层级树或定位表，论文路线优先问题-方法-角色矩阵，学习向导优先课程-能力-论文桥接表，真实流程或平台链路才用小型 Mermaid 或文本链路。

## 当前样本状态

### 张鹏举

位置：

```text
output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举
```

状态：

- 已有 `00-04` 五份成品文档。
- 已有 `_internal/blueprint.md` 和 `_internal/evidence/key_claims.md`。
- 当前目标论文是 2025 年液体水 ICD 竞争机制论文，回看论文是 2022 年液体水 ICD 首次观测论文。
- `04_学习向导.md` 已从高数、线代、普物接到光电子谱、液体水 ICD、核心图、平台链路和进组问题。
- `02_领域地图.md` 已补“从领域地图接到目标论文”，把目标论文、回看论文、核心图、平台链路和学习桥显式接到 `03/04`。

剩余风险：

- 固体/关联电子材料分支公开论文链较短，只能作为正在形成的分支。
- 2026 年 SF6 论文存在单篇 DOI 元数据 ORCID 差异，不能改写主体身份，仍需人工复核。
- 机械验证通过不代表 2025/2022 液体水 ICD 的所有图和模型解释已经完成专家级学术审查。

### 汪非凡

位置：

```text
output/中国科学院大学/中科院物理研究所/超快物质科学中心/汪非凡
```

状态：

- 已从官网和机构路径重新跑出新 `_internal/` 状态，不能把旧回归或 archive 当证据。
- `risk_gate.py --prof-dir` 已恢复到 `mode: standard`。
- 已有 `00-04` 五份成品文档。
- 已有 `_internal/blueprint.md` 和 `_internal/evidence/key_claims.md`。
- 当前目标论文是 2023 年 Science Advances 的 `Nonlinear terahertz control of the lead halide perovskite lattice`。
- `00/04` 已补自包含读者定位、起步讨论入口、目标论文核心图链、Zenodo 数据复核入口和平台瓶颈判断。
- `03/04` 已把 Fig. 1A/B/C 和 Fig. 4A-F 的读图链接到 arXiv 开放 PDF 与 Zenodo 数据集。
- `02_领域地图.md` 已补“从领域地图接到目标论文”，把目标论文、核心图、平台链路、当前扩展方向和学习桥显式接到 `03/04`。

剩余风险：

- 2D-OKE、Kerr 和极化子论文的核心图尚未逐图复核。
- 官网中的 SHG microscopy、二维铁电材料 THz/相干声子和固体 HHG/阿秒方向部分写为 in revision / in preparation，只能作为方向线索。
- Google Scholar 中跨领域或无 DOI 条目仍只作为待复核噪声，不用于当前主线判断。

## 最近里程碑

最近提交：

```text
本轮变更 docs: make openalex email fully optional
本轮变更 docs: keep openalex email optional
本轮变更 docs: clarify quickstart phase a bridge
本轮变更 docs: align material guide template deliverables
本轮变更 docs: avoid mcp-specific phase1 search wording
本轮变更 docs: fix phase1 plan data flow order
本轮变更 docs: align identity locking with autonomous risk gate
本轮变更 fix: forbid four-deliverable wording in final docs
本轮变更 docs: clarify phase1 collection before risk gate
本轮变更 docs: align active docs to five deliverables
本轮变更 fix: forbid key paper labels in final docs
ff3d2cfa chore: ignore local codex noise dirs
118005ee docs: advertise five advisor deliverables
2f91e92e docs: avoid forbidden phase4 reference wording
0da1a9d1 docs: align skill name format contract
870bc077 fix: hide legacy archive cli option
ca11d6eb fix: keep phase1 init stdout clean
f24910f2 fix: print prof dir from phase1 init
3754f62e fix: drop root latest fallback
e8e8dd83 docs: strengthen wang learning bridge
本轮变更 chore: remove legacy archive migration entrypoint
本轮变更 docs: make claude config defer to agents
本轮变更 chore: remove obsolete run.py entrypoint
本轮变更 docs: align render profile contract
本轮变更 docs: remove local reference version footers
本轮变更 docs: prefer prof-dir in phase1 usage examples
本轮变更 docs: align data-source examples with prof-dir
本轮变更 docs: drop legacy plan footer
本轮变更 docs: remove obsolete helper recovery path
47cb6e4a feat: add compact mermaid visuals
本轮变更 chore: remove obsolete phase1 helpers
本轮变更 fix: canonicalize known institute alias
e8ae5bd docs: connect field maps to blueprints
68c5103 fix: enforce bilingual title spacing
aaaa970 fix: keep career stage drafts internal
e623373 docs: demote legacy archive entrypoint
9f73972 feat: let phase1 validators use prof dir
```

这些提交分别解决：

- 本轮变更继续收紧 OpenAlex 邮箱输入边界：`pipeline.md` 的“邮箱与限速”小节不再写“推荐使用真实邮箱”，而是说明邮箱只能来自执行环境预配置；无配置时走约 1 req/s 的可接受降级，不得向用户索要。入口测试新增禁用短语，防止未来把维护者邮箱重新写成用户输入。
- 本轮变更把 OpenAlex 示例中的占位邮箱 `your@real.com` / `your@email.com` / `you@example.com` 移除；`step3_openalex.py`、`pipeline.md` 和 `01-data-sources.md` 默认示例都只用 `--prof-dir`。邮箱只作为执行环境预配置的可选 polite-pool 参数，不得向用户索要，保持三行最小输入契约。新增入口测试防止占位邮箱回流。
- 本轮变更在 `QUICKSTART.md` 的常规执行骨架中补上 `phase1_init.py` 之后、Phase B 采集脚本之前的 Phase A 桥接：从 `_internal/seed.json` 的官网 URL 出发，检索官网、GS、OpenAlex、ORCID，交叉验证身份，并写入 `00_verified_ids.json` 与 `career_stages.json`。新增入口测试防止 quickstart 只说 init 和 step2/3/4/6，却漏掉身份锁定和阶段配置。
- 本轮变更把 `assets/templates/00_材料导读.md` 的文件定位表从只列 `01-04` 修正为 `00-04` 五份文档，并把开头说明从“四个层面”改成“五个层面”。新增模板测试防止后续生成新导师导读时再次漏掉 `00_材料导读.md` 自身。
- 本轮变更把 `phase1-core.md` 和 `src/phase1/pipeline.md` 中“必须用 MCP 搜 / MCP fetch”的工具限定，改成“当前可用的网页读取和检索工具”。证据顺序仍是官网、Google Scholar、OpenAlex、ORCID 和交叉验证，但不再让 Codex、Claude 或其他执行环境因为工具名不同而误判无法执行。新增入口测试防止 Phase 1 文档再次硬编码 MCP 搜索工具。
- 本轮变更修正 `docs/计划书.md` 附录 C.5 的阶段 1 数据流顺序：先用 `phase1_init.py` 建立 active `prof_dir`、`_internal/latest.txt` 和 `seed.json`，再从 seed 的官网 URL 进入 Phase A 身份锁定和阶段配置，随后 Phase B 采集、risk gate、Phase C 渲染验证。新增入口测试防止计划书再把 Phase A 广域搜索写在初始化之前。
- 本轮变更把 `docs/计划书.md` 中残留的“人闸 / 身份消歧不允许全自动 / ID 锁定前用户确认”旧设计改成当前基线：默认从三行输入自动推进，身份锁定依赖官网邮箱、ORCID、论文指纹和合著者/机构网络证据层级；证据不足时由 `risk_gate.py` 输出 `conservative_required` 和 `next_actions`，再补官方来源、剔除噪声论文或标 `需人工复核`。新增入口测试防止计划书再次退回默认人工确认。
- 本轮变更把最终成品中的“这四份文档”旧口径纳入 `verify_phase_docs.py` 禁用写作风格，并把张鹏举、汪非凡 `00_材料导读.md` 的文件定位表补成 `00-04` 五份文档。这个改动只解决成品数量口径和导读自包含问题，不改变论文事实、目标论文选择或学习路线判断。
- 本轮变更在 `QUICKSTART.md` 明确 `phase1_init.py` 只创建目录和 seed，不会采集论文；小白执行 AI 不能从 init 直接跳到 `risk_gate.py`，必须先按 `pipeline.md` / `phase1-core.md` 跑 `step2_gs.py`、`step3_openalex.py`、`step4_arxiv_id.py` 或 `step5_arxiv.py`、`step6_merge.py`。新增入口测试防止 quickstart 再次漏掉这条。
- 本轮变更把 `AGENTS.md`、`docs/计划书.md` 和 `docs/batches/` 输入清单中残留的“四份文档”旧口径统一为 `00-04 五份文档`，并新增入口测试防止活跃规则、设计文档和批量输入清单再次把交付物说回四份。
- 本轮变更把最终 00-04 成品中的“关键论文”纳入 `verify_phase_docs.py` 禁用写作风格，并把张鹏举 00/02/03/04 参考表里的该标签改为“目标论文”或“当前主线论文”。这个改动解决的是成品筛选式标签的底线问题，不改变论文事实、链接或正文判断。
- `ff3d2cfa` 把本地 Codex 状态目录 `.codex/` 和未纳入项目的 `精髓转移/` 加入 `.gitignore`，减少 full `git status` 噪声；这些目录仍不提交、不作为项目资产。
- `118005ee` 把 `research-advisor` skill 元数据从旧 “four Markdown deliverables” 改成 “five Markdown deliverables”，并在触发文档列表里补回 `00_材料导读.md`。
- `2f91e92e` 把 `phase4-learning-guide.md` 中容易被执行 AI 复用进正文的“关键论文”改成“目标论文或当前主线论文”，避免内部 reference 使用最终文档禁用措辞。
- `0da1a9d1` 把 `research-advisor/SKILL.md` 的最终画像姓名契约从旧 `中文名(English Name)` 对齐为 `中文名 (English Name)`，并新增入口测试防止 skill 与 verifier/成品标题规范再次分叉。
- `870bc077` 隐藏 Phase 1 采集、合并、渲染和验证脚本的旧 `--archive-dir` 公开 help，并把可见文案统一改为 `--prof-dir` / active state。旧参数仅保留内部兼容，不再作为小白执行 AI 可见入口。
- `ca11d6eb` 继续收紧 `phase1_init.py` 成功路径：stdout 只输出 `prof_dir`，stderr 为空。archive 路径只通过 `_internal/latest.txt` 和 `ProfDirResolver` 内部解析，不再作为用户可见成功输出，避免 PowerShell 把非错误 stderr 包装成误导性错误信息。
- `f24910f2` 先把 `phase1_init.py` 的 stdout 主输出从 `_internal/archive/<ts>` 改为 `prof_dir`，与 `research-advisor` 和 `QUICKSTART.md` 的“后续统一使用 --prof-dir”契约一致；后续 `ca11d6eb` 又删除了成功路径 stderr 诊断。
- `3754f62e` 删除 `ProfDirResolver` 对导师根目录 `latest.txt` 的过渡兼容读取，只允许从 `_internal/latest.txt` 解析 active state；`src/phase1/pipeline.md` 同步删除旧兼容说明，并新增测试防止工具层继续给根目录机器文件留后门。
- `e8e8dd83` 只处理汪非凡非黄金样本的成品质量回归：补强 `00_材料导读.md` 和 `04_学习向导.md`，让它们像张鹏举样本一样说明小白读者起点、目标论文核心图、开放数据复核、平台链路和进组前可请教问题。该提交没有新增全局规则，避免把单样本表达问题升格为 harness 臃肿。
- 删除旧整目录迁移脚本 `src/phase1/archive_previous.py`，并从 `src/phase1/pipeline.md` 移除旧版整目录存档说明。当前只保留导师目录内 `_internal/archive/<ts>` 作为 active state 快照；项目级 `archive/` 仍是人工溯源区，Agent 不读也不把它当 workflow 入口。
- `CLAUDE.md` 已降级为 Claude Code 兼容指针，不再复制硬约束、目录角色或反臃肿规则。当前项目规则的单一事实源是 `AGENTS.md`，新窗口仍按 `AGENTS.md`、`QUICKSTART.md` 和本文件恢复，避免两套 agent 配置逐步分叉。
- 删除旧统一编排脚本 `src/phase1/run.py`，并从 `QUICKSTART.md`、`docs/计划书.md` 和 `src/phase1/pipeline.md` 移除相关入口说明。Phase 1 不再保留一条会手动拼 `archive` / `-o` 的平行路线；小白执行 AI 只能从 `phase1_init.py` 建 active state，再用 `--prof-dir` 逐步调用脚本。
- `render_profile.py` 顶部说明已对齐当前 Phase 1 契约：脚本自动生成 §1/§2/§4/§8，AI 只补 §3/§4 叙事/§5/§6/§7/§9 占位符；不再写“5 年默认段”或“脚本不写学术履历表格”这类旧说法。
- `research-advisor/references/` 不再保留局部版本尾注；active reference 的版本追溯以 git 历史、`docs/上下文交接.md` 和 `docs/计划书.md` 为准，避免多套“最新版本”并存。
- 阶段 1 采集、合并和渲染脚本 docstring 中的默认用法示例已改为 `--prof-dir output/...`，保留旧参数兼容但不再把小白执行 AI 引向手动拼 `-o` 输出路径；测试已覆盖脚本 usage 示例不能回退。
- `01-data-sources.md` 的 Google Scholar / OpenAlex / arXiv 示例已从旧 `-o 01/02/03_*.json` 改为 `--prof-dir "output/..."`，避免执行 AI 绕开 active state 写散落中间文件；测试已覆盖这些旧示例不能重新出现。
- `docs/计划书.md` 已删除末尾遗留的 v5.0 版本尾注，避免新窗口误判当前设计版本；测试已覆盖旧版本尾注不能重新出现。
- `phase1-recovery.md` 已移除对已删除 `archive_step.py` 的恢复命令引用。恢复流程只允许重新执行缺失中间产物对应的 step，并用 `--prof-dir` 让 `ProfDirResolver` 写入当前 active state。
- 张鹏举和汪非凡的 `02-04` 已把原来的纯文本箭头链替换为小型 Mermaid `flowchart TD`。这轮只补“领域定位 / 论文问题链 / 学习桥”三类理解图，不把表格、矩阵和正文强行流程图化。
- `verify_mermaid_render.py` 已补 Mermaid 渲染 smoke。默认优先找本地 `mmdc`；没有时可用 `--use-npx` 调 `@mermaid-js/mermaid-cli`。脚本会写无 BOM 的 Puppeteer 配置，并传入 `--no-sandbox --disable-setuid-sandbox`，避免 Windows/受限 Chromium 环境下渲染超时或浏览器直接关闭。
- `phase1_init.py` 的结构化路径构造已把已知别名 `中科院物理所` 归一化为 `中科院物理研究所`，避免同一机构生成两套导师目录。
- 删除无活跃引用的 `archive_step.py` 和 `merge_tables.py`，避免旧的自动归档/手动表格合并路径继续作为平行入口误导执行 AI。
- `02_领域地图.md` 显式消费蓝图，连接目标论文、核心图、平台链路和学习桥。
- 成品一级标题姓名格式统一为 `中文名 (English Name)`。
- `gen_career_stages_draft.py --prof-dir` 默认输出到 `_internal/`。
- Phase A 格式自检脚本补齐 `--prof-dir`。
- `docs/e2e/` 和 `docs/batches/` 已标记为历史记录/输入清单，不能作为 workflow 入口；`docs/计划书.md` 的 checkpoint 路径已统一为 `docs/上下文交接.md`。

## 最近验证状态

本轮 OpenAlex 邮箱限速说明已按 TDD 跑通：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_phase1_docs_do_not_prompt_user_for_openalex_email
rg -n -g "*.md" -g "*.py" -g "!**/archive/**" -- "推荐使用真实邮箱|真实邮箱（非|your@real\\.com|your@email\\.com|you@example\\.com" src/phase1/pipeline.md src/phase1/step3_openalex.py .claude/skills/research-advisor/references/01-data-sources.md tests/test_project_entrypoints.py
```

结果：

- 收紧后的红灯测试先失败，证明 `pipeline.md` 仍写着“推荐使用真实邮箱”，会把可选维护者邮箱误导成用户输入。
- 修正后该小节明确：邮箱只能来自执行环境预配置；没有配置时按无 email 路径限速到约 1 req/s，属于可接受降级。
- `rg` 只在测试自身命中禁用短语，相关执行文档不再出现这些误导口径。
- 该验证只证明 OpenAlex 邮箱不再破坏三行最小输入契约，不证明无 email 网络请求不会遇到限速、503 或外部服务波动。

本轮 OpenAlex 邮箱最小输入契约已按 TDD 跑通：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_phase1_docs_do_not_prompt_user_for_openalex_email
rg -n -- "your@real\\.com|your@email\\.com|you@example\\.com" src/phase1/pipeline.md src/phase1/step3_openalex.py .claude/skills/research-advisor/references/01-data-sources.md
```

结果：

- 新增入口红灯测试先失败，证明 Phase 1 示例仍保留占位邮箱，会诱导执行 AI 向用户索要第四项输入。
- 修正后默认 OpenAlex 示例不带 `--email`；预配置维护者邮箱只作为可选 polite-pool 参数；测试通过。
- `rg` 无匹配，相关执行文档和脚本说明不再出现占位邮箱。
- 该验证只证明输入契约收敛，不证明无 email 时 OpenAlex 网络请求一定不会遇到限速或服务端波动。

本轮 quickstart Phase A 桥接已按 TDD 跑通：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_quickstart_mentions_phase_a_before_phase_b_scripts
```

结果：

- 新增入口红灯测试先失败，证明 `QUICKSTART.md` 只说明 init、step2/3/4/6 和 risk gate，没有在 init 后显式接上 Phase A 身份锁定与阶段配置。
- 修正后 quickstart 明确 init 只建目录和 seed；随后先完成 Phase A 并写入 `00_verified_ids.json`、`career_stages.json`，再跑采集、合并和风险门；测试通过。
- 该验证只证明入口说明顺序完整，不证明身份锁定检索在所有网络环境下都能自动成功。

本轮材料导读模板五份口径收敛已按 TDD 跑通：

```text
python -m unittest discover -s .claude/skills/research-advisor/tests -p "test_verify_phase_docs.py" -v -k test_material_guide_template_lists_all_five_deliverables
```

结果：

- 新增模板红灯测试先失败，证明 `00_材料导读.md` 模板仍只列 `01-04`，没有把导读自身纳入文件定位。
- 修正后模板列出 `00-04` 五份文档，并在开头使用“五份材料/五个层面”口径；测试通过。
- 该验证只证明模板骨架不会再次漏掉 `00_材料导读.md`，不证明模板填充后的具体导师内容质量。

本轮 Phase 1 检索工具措辞收敛已按 TDD 跑通：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_phase1_docs_do_not_require_mcp_specific_search_tools
rg -n "MCP 搜|MCP fetch" AGENTS.md QUICKSTART.md docs/计划书.md .claude/skills/research-advisor src/phase1/pipeline.md --glob "*.md" --glob "!archive/**" --glob "!output/**/archive/**"
```

结果：

- 新增入口红灯测试先失败，证明 Phase 1 活跃文档仍把搜索工具写死为 MCP。
- 修正后 Phase 1 文档要求按官网、GS、OpenAlex、ORCID 和交叉验证顺序检索，但工具可用 Web search、浏览器或 Claude MCP；测试通过。
- `rg` 无匹配，Phase 1 执行入口和 reference 中不再出现 `MCP 搜` 或 `MCP fetch`。
- 该验证只证明工具措辞不再绑死执行环境，不证明任一搜索工具在网络或反爬限制下必然成功。

本轮 Phase 1 数据流顺序收敛已按 TDD 跑通：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_plan_phase1_data_flow_initializes_before_identity_search
rg -n "AI 将 Phase A 的 00_verified_ids.json|Phase A：AI 广域搜索|产出存 archive/<ts>" docs/计划书.md
```

结果：

- 新增入口红灯测试先失败，证明计划书附录 C.5 仍把 Phase A 广域搜索放在 `phase1_init.py` 之前。
- 修正后数据流改为 `phase1_init.py -> Phase A 身份锁定 -> Phase B 采集/risk gate -> Phase C 渲染/verify`；测试通过。
- `rg` 无匹配，旧逆序描述不再保留。
- 该验证只证明设计文档顺序对齐，不证明外部检索、API 返回和身份/论文归属判断总能自动成功。

本轮身份锁定自动化口径收敛已按 TDD 跑通：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_plan_does_not_require_default_human_gate_for_identity_locking
rg -n "人闸|身份消歧不允许全自动|任何 ID 锁定前用户确认" docs/计划书.md AGENTS.md QUICKSTART.md .claude/skills/research-advisor src/phase1/pipeline.md --glob "*.md" --glob "!archive/**" --glob "!output/**/archive/**"
```

结果：

- 新增入口红灯测试先失败，证明 `docs/计划书.md` 仍保留旧“默认人工确认 ID”设计口径。
- 修正后计划书改为“自动身份锁定 + 证据分层 + `risk_gate.py` conservative 恢复”的当前基线；测试通过。
- `rg` 无匹配，活跃入口和执行文档不再保留这组旧人工闸口措辞。
- 该验证只证明设计文档口径收敛，不证明身份锁定算法对所有同名学者都已达到专家级准确率。

本轮成品五份口径 smoke 已按 TDD 跑通：

```text
python -m unittest discover -s .claude/skills/research-advisor/tests -p "test_verify_phase_docs.py" -v -k test_rejects_four_deliverable_ladder_wording_in_final_docs
python .claude/skills/research-advisor/scripts/verify_phase_docs.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举"
python .claude/skills/research-advisor/scripts/verify_phase_docs.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/汪非凡"
rg -n "这四份文档|四份互不相干|四份文档是一条|四份文档是认知" output --glob "!**/archive/**"
```

结果：

- 新增 verifier 红灯测试先失败，再通过；`verify_phase_docs.py` 现在会拦截最终成品中把认知阶梯说成“这四份文档”的口径漂移。
- 张鹏举、汪非凡 `00_材料导读.md` 的文件定位表已补入 `00_材料导读.md` 自身，并把说明改为 `00-04` 五份文档共同构成认知阶梯。
- 张鹏举、汪非凡 `verify_phase_docs.py` 均通过。
- `output/` 当前成品中不再出现该旧口径。
- 该验证只证明成品数量口径和导读文件定位收敛，不证明论文事实、核心图解释或学习路线已经完成专家级学术审查。

最近一次工作树噪声、skill 元数据、reference 入口措辞与 CLI help 入口收敛验证：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_gitignore_excludes_local_codex_noise_dirs
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_research_advisor_skill_advertises_five_deliverables
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_phase4_reference_avoids_forbidden_key_paper_wording
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_research_advisor_skill_uses_spaced_bilingual_name_contract
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_phase1_cli_help_does_not_expose_archive_dir
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v
python -m unittest discover -s tests -p "test_*.py" -v
python -m unittest discover -s .claude/skills/research-advisor/tests -p "test_*.py" -v
git diff --check -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**' ':(exclude).codex/**' ':(exclude)精髓转移/**'
```

结果：

- 新增 CLI help 防回归测试先失败，再通过；公开 `--help` 中不再暴露 `--archive-dir`、`archive_dir` 或小写 `archive`。
- 新增 skill 姓名格式防回归测试先失败，再通过；`research-advisor/SKILL.md` 不再写无空格 `中文名(English Name)`。
- 新增 Phase 4 reference 措辞防回归测试先失败，再通过；`phase4-learning-guide.md` 不再把“关键论文”作为写作目标。
- 新增 skill 元数据防回归测试先失败，再通过；`research-advisor/SKILL.md` 不再写 `four Markdown deliverables`，且显式包含 `00_材料导读.md`。
- 新增 `.gitignore` 防回归测试先失败，再通过；完整 `git status` 不再显示 `.codex/` 和 `精髓转移/`。
- `test_project_entrypoints.py` 26 个通过。
- 根目录 unittest 80 个通过。
- `research-advisor` skill unittest 53 个通过。
- `git diff --check` 无输出。

本轮五份交付物口径收敛已按 TDD 跑通：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_active_docs_do_not_describe_deliverables_as_four_docs
rg -n "四份文档" AGENTS.md QUICKSTART.md docs/计划书.md docs/batches .claude/skills/research-advisor/SKILL.md .claude/skills/research-advisor/references -g "*.md" -g "!archive/**" -g "!output/**/archive/**"
```

结果：

- 新增入口红灯测试先失败，先命中 `AGENTS.md` 和 `docs/计划书.md`，扩展到 `docs/batches/` 后又命中批量清单；修正后通过。
- 活跃入口、设计文档、批量输入清单、skill 和 references 中不再出现“四份文档”旧口径。
- 该验证只证明交付物数量口径收敛，不证明五份成品内容质量。

本轮 Phase 1 quickstart 防误跳已按 TDD 跑通：

```text
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_quickstart_warns_not_to_jump_from_init_to_risk_gate
```

结果：

- 新增入口红灯测试先失败，证明 `QUICKSTART.md` 没有明确禁止从 `phase1_init.py` 直接跳到 `risk_gate.py`。
- 修正后 quickstart 明确 init 只创建目录和 seed；执行阶段 1 必须先跑采集与合并脚本，再运行风险门。
- 该验证只证明入口说明不会漏掉 Phase B/C 顺序，不证明外部 API 检索结果或身份/论文归属判断正确。

本轮成品筛选标签 smoke 已按 TDD 跑通：

```text
python -m unittest discover -s .claude/skills/research-advisor/tests -p "test_*.py" -v -k test_rejects_key_paper_label_in_final_docs
python .claude/skills/research-advisor/scripts/verify_phase_docs.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举"
rg -n "关键论文" -g "!archive/**" -g "!output/**/archive/**" -g "!.codex/**" -g "!精髓转移/**"
```

结果：

- 新增 verifier 红灯测试先失败，再通过；`verify_phase_docs.py` 现在会拦截最终成品里的“关键论文”筛选式标签。
- 张鹏举 `00/02/03/04` 四份成品参考表已把“关键论文”改为“目标论文”或“当前主线论文”，`verify_phase_docs.py` 通过。
- `rg` 中剩余“关键论文”只出现在测试、禁用规则说明、历史交接记录或 Phase 1 表格禁用示例中；不再出现在张鹏举成品正文。
- 这只证明机械 smoke 能拦截该标签，不证明目标论文选择、图示解释或论文路线已经完成专家级学术审查。

当前 Mermaid 图示里程碑提交前已复跑：

```text
python .claude/skills/research-advisor/scripts/verify_phase_docs.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举"
python .claude/skills/research-advisor/scripts/verify_phase_docs.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/汪非凡"
python .claude/skills/research-advisor/scripts/verify_mermaid_render.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举" --use-npx
python .claude/skills/research-advisor/scripts/verify_mermaid_render.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/汪非凡" --use-npx
python .claude/skills/research-advisor/scripts/verify_source_metadata.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举"
python .claude/skills/research-advisor/scripts/verify_source_metadata.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/汪非凡"
python -m unittest discover -s tests -p "test_*.py" -v
python -m unittest discover -s .claude/skills/research-advisor/tests -p "test_*.py" -v
git diff --check -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**'
```

结果：

- 张鹏举、汪非凡 `verify_phase_docs.py` 均通过。
- 张鹏举、汪非凡 `verify_mermaid_render.py --use-npx` 均渲染 3 个 Mermaid 图示。
- 张鹏举、汪非凡 DOI metadata smoke 本轮均通过。
- 根目录 unittest 65 个通过。
- `research-advisor` skill unittest 53 个通过。
- `git diff --check` 无输出。

已观察到的元数据风险：汪非凡目标论文 DOI `10.1126/sciadv.adg3856` 曾偶发返回 metadata unavailable 警告，这属于外部元数据服务不可用，不是标题错配。若后续复跑出现 `[FAIL] DOI title mismatch`，必须先修正文档或来源表，不能提交。

汪非凡 `00/04` 补强提交前已复跑：

```text
python .claude/skills/research-advisor/scripts/verify_phase_docs.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/汪非凡"
python .claude/skills/research-advisor/scripts/verify_mermaid_render.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/汪非凡" --use-npx
python .claude/skills/research-advisor/scripts/verify_source_metadata.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/汪非凡"
python .claude/skills/research-advisor/scripts/verify_phase_docs.py --prof-dir "output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举"
git diff --check -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**' ':(exclude).codex/**' ':(exclude)精髓转移/**'
```

结果：

- 汪非凡 `verify_phase_docs.py` 通过。
- 汪非凡 `verify_mermaid_render.py --use-npx` 渲染 3 个 Mermaid 图示。
- 汪非凡 DOI metadata smoke 通过。
- 张鹏举 `verify_phase_docs.py` 回归通过。
- `git diff --check` 无输出。
- 这只证明机械结构、Mermaid 渲染和 DOI 元数据没有发现错误；2D-OKE、Kerr、polaron 等非目标论文核心图仍需人工逐图学术审查。

根目录 `latest.txt` 兼容删除提交前已按 TDD 复跑：

```text
python -m unittest discover -s tests -p "test_phase1_harness.py" -v -k test_prof_dir_resolver_ignores_legacy_root_latest
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_pipeline_does_not_document_root_latest_compatibility
python -m unittest discover -s tests -p "test_*.py" -v
python -m unittest discover -s .claude/skills/research-advisor/tests -p "test_*.py" -v
git diff --check -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**' ':(exclude).codex/**' ':(exclude)精髓转移/**'
```

结果：

- 两个新增红灯测试先失败，分别证明旧 resolver 会读取根目录 `latest.txt`，且 `pipeline.md` 还记录旧兼容；修改后两者通过。
- 根目录 unittest 74 个通过。
- `research-advisor` skill unittest 53 个通过。
- `git diff --check` 无输出。
- 该验证只证明工具路径和文档入口收紧，不证明 Phase 2-4 学术内容质量。

`phase1_init.py` stdout 改为 `prof_dir` 提交前已按 TDD 复跑：

```text
python -m unittest discover -s tests -p "test_phase1_harness.py" -v -k test_phase1_init_writes_state_inside_internal_dir
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_phase1_init_stdout_is_not_archive_entrypoint
python -m unittest discover -s tests -p "test_*.py" -v
python -m unittest discover -s .claude/skills/research-advisor/tests -p "test_*.py" -v
git diff --check -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**' ':(exclude).codex/**' ':(exclude)精髓转移/**'
```

结果：

- 两个契约测试先失败，分别证明 stdout 仍是 archive 路径、源码仍把 archive 作为主输出；修改后两者通过。
- 根目录 unittest 75 个通过。
- `research-advisor` skill unittest 53 个通过。
- `git diff --check` 无输出。
- 该验证只证明 Phase 1 初始化入口输出收紧，不证明后续 API 检索或学术内容质量。

`phase1_init.py` 成功路径 stderr 清理提交前已复跑：

```text
python -m unittest discover -s tests -p "test_phase1_harness.py" -v -k test_phase1_init
python -m unittest discover -s tests -p "test_project_entrypoints.py" -v -k test_phase1_init_stdout_is_not_archive_entrypoint
python -m unittest discover -s tests -p "test_*.py" -v
python -m unittest discover -s .claude/skills/research-advisor/tests -p "test_*.py" -v
git diff --check -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**' ':(exclude).codex/**' ':(exclude)精髓转移/**'
```

另做 UTF-8 初始化 smoke：

```text
phase1_init.py --university 测试大学 --institute 测试所 --department 测试部门 --name 张三 --official-url https://example.edu/teacher/zhangsan
```

结果：

- stdout 为 `output/测试大学/测试所/测试部门/张三`。
- `_internal/latest.txt` 和 `_internal/seed.json` 均存在。
- stderr 为空。
- 根目录 unittest 75 个通过，`research-advisor` skill unittest 53 个通过，`git diff --check` 无输出。
- 该验证只证明初始化入口和路径契约，不证明外部 API 检索或学术内容质量。

## 当前工作树注意

`git status --short --branch` 仍可能显示：

```text
.codex/
精髓转移/
output/**/archive/** 删除或变动
```

处理方式：

- `.codex/` 不提交。
- `精髓转移/` 未明确纳入项目，不提交。
- `output/**/archive/**` 的删除或变动禁止读取、恢复或提交。

## 下一步建议

1. 继续项目瘦身：审计 `docs/`、`.claude/skills/research-advisor/`、`src/phase1/pipeline.md` 和脚本入口，删除或降级已经被主路径吸收的平行入口；`--archive-dir` 公开 help 已处理，除非有失败证据，不要重复审计同一入口。
2. 做一次“新窗口可执行性”演练：只读 `AGENTS.md`、`QUICKSTART.md`、本文件和 `research-advisor/SKILL.md`，检查能否从三行输入推导下一步，不依赖聊天记忆。
3. 继续对张鹏举和汪非凡做轻量人工式质量自审：已发现并补强汪非凡 `00/04` 的自包含密度；后续若再发现单样本表达问题，仍先改成品，不立刻升格成全局规则。
4. 若继续回归，优先选择一个从最小输入重新跑的新导师，检验端到端入口，而不是只在已有成品上润色。
5. 每个里程碑结束时运行相关验证、独立提交、更新本文件，并明确 smoke 不能证明的学术质量风险。
