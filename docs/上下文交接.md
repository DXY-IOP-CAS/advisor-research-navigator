# 上下文交接 — 新窗口任务：审核计划 + 实施阶段 1 重构

## 窗口纪要（2026-07-01）

### 做了什么

1. **系统性调研了 4 个候选工具 + 6 个额外 MCP 项目**
   - 结论：academic-research-mcp、google-scholar-search-mcp 不采用。scholarly 参考用但不做主方案。professor-fit-analyser 留待阶段 2/3。
   - **核心发现**：GS 封锁不是换库能解决的。真实浏览器（Playwright stealth）+ 住宅 IP + 慢节奏 + 缓存 = 全自动 $0 方案。
   - 社区调研：Publish or Perish（200 万用户）、Zotero（千万用户）都用"慢速 + 缓存 + CAPTCHA 弹窗"策略，没有人在不付费的情况下 100% 避免封锁。
   - 调研报告：`docs/调研_阶段1工具选型.md`（v2.0，已更新）

2. **项目结构标准化**
   - `scripts/` → `src/`（+ `__init__.py`）
   - `计划/` → `docs/`
   - `项目/` → `output/`
   - `存档/` → `archive/`（50 份文件全部保留，0 删除）
   - 旧空目录已清除。`.claudeignore`、`CLAUDE.md`、`QUICKSTART.md` 已更新。
   - 两次提交校验：`5b9d4aa` + `b864c8e`

3. **GS 封锁现状确认**
   - 当前 IP（Windows 11 家庭网络）已被 GS HTTP 403 封锁
   - 张鹏举实测：GS 0 篇（403 blocked）、OpenAlex 14 篇（预期 ~50+ 篇）、arXiv 有 URL 编码 bug
   - 封锁是暂时性的（社区报告 1-24 小时恢复），但重启脚本又会封

### 关键决策

| 决策 | 内容 | 依据 |
|:-----|:-----|:------|
| GS 方案 | Playwright stealth + 真实 Chrome profile（非付费代理） | browser 方案已通过 CreepJS/Cloudflare 验证（agent-browser-stealth） |
| GS 策略 | 10-15s 间隔，5 位/批，批间暂停 5 分钟 | Publish or Perish 用 ~6 次/分钟跑 GS 200 万用户验证可行 |
| 多源 | GS 主源 + OpenAlex 元数据 + arXiv 预印本 | OA 中文覆盖 22-38%，GS 覆盖灰色文献 |
| 缓存 | 30 天 TTL，支持断点续跑 | 一次跑完不再重复请求 GS |
| 付费 | 不引入付费代理 | $0 方案（真实浏览器 + 慢节奏）可行 |
| 架构 | 保留 6 个脚本，替换 1 个，新增 2 个 | 见 docs/阶段1重构计划.md |

### 产出清单

- `docs/调研_阶段1工具选型.md` — 完整调研报告（v2.0）
- `docs/阶段1重构计划.md` — 重构实施方案（Phase A/B/C）
- `docs/上下文交接.md` — 本文档

### 待用户审核

用户需先审核 `docs/阶段1重构计划.md`，确认无大问题后告诉 AI "开干"。

### 下一步任务

```
Phase A: 基础设施
  1. pip install playwright + 验证 Chrome 启动
  2. 写 src/cache_manager.py（缓存读写 + TTL）
  3. 写 src/gs_browser.py（Playwright stealth 爬 GS profile）
  4. 单测：用张鹏举 GS ID 跑一次

Phase B: 流水线组装
  5. 修复 src/arxiv_preprints.py（URL 编码 bug）
  6. 写 src/batch_run.py（批量流水线入口）
  7. 端到端测试 2-3 位导师

Phase C: 批量生产（用户确认后）
  8. 整理物理所导师名单（~80-120 人）
  9. 跑全量批量
  10. 验收输出质量
```

### 测试用身份

```
张鹏举 / Pengju Zhang
中科院物理所 (IPHY)
GS: ls7XuGoAAAAJ
OpenAlex: A5000914228
ORCID: 0000-0001-7746-2113
官网: http://english.iphy.ac.cn/ (或其他)
```

### 项目路径要点

- 源文件：`src/`（Python 脚本，不是 `scripts/`）
- 文档：`docs/`（规划 + 调研 + 计划）
- 输出：`output/导师/<姓名>/`（画像文件）
- 缓存（新建）：`.cache/gs/`、`.cache/oa/`、`.cache/arxiv/`（gitignored）
- 引用文件跑 `python src/<filename>.py`（不是 `python scripts/...`）

### 注意事项

- CLAUDE.md 规则参见项目根 `CLAUDE.md` 和 `docs/计划书.md`
- 项目规则优先级：docs/计划书.md > CLAUDE.md > QUICKSTART.md
- `archive/` 在 `.claudeignore` 中排除，不会加载到上下文
- 旧路径（scripts/、计划/、项目/、存档/）已清理，不要再用
