# advisor-research-navigator 上下文交接 - 2026-07-08

本文件只记录当前状态、风险和下一步。历史细节用 `git log`、提交 diff 和 `docs/e2e/` 回归记录追溯；不要把这里重新写成长流水账。

## 热启动区：新窗口先看这里

这个窗口已经不适合继续做内容工作。新窗口接手时，以 Git、`AGENTS.md`、`QUICKSTART.md` 和本节为准；不要复盘本窗口聊天，不要从旧 S4 或旧 SVG 重新开始。

当前分支和最近提交：

```text
当前分支：codex/learning-folder-guardrails
7e8210f Add learning folder guardrails
f2862ea docs: refresh handoff after S4 milestone
112de06 docs: rebuild S4 visualization method
```

当前项目路线：

```text
S1-S4 支撑方法
-> M0 总方法论
-> D00-D04 文档标准
-> 真实导师成品
-> Harness / skill / 模板 / verifier / tests
```

不要跳层。旧模板、旧 verifier 和旧 skill reference 不能提前固化尚未稳定的方法。

`00-04` 五份成品的职责锚点如下。后续 M0、D00-D04、真实样本和 Harness 都要按这张表校准；如果这里理解歪，后面会越做越偏。

| 成品 | 作用 | 读完应能做到 | 常见跑偏 |
|:---|:---|:---|:---|
| `00_材料导读.md` | 教学生使用整套材料 | 知道阅读顺序、回看点、自检方式和来源核查方式 | 写成项目说明、目录说明或摘要 |
| `01_基础画像.md` | 建立事实底座 | 知道老师是谁、哪些资料稳、履历和论文集合怎样读 | 写成简历介绍或领域解释 |
| `02_领域地图.md` | 建立专业范围感 | 知道导师方向处在哪片领域、旁边哪些方向容易混淆、先抓哪些概念 | 写成泛领域综述，或提前替 `03` 选择目标论文 |
| `03_论文路线.md` | 建立论文问题链 | 能复述论文群怎样分工推进当前方向，哪条关系仍需复核 | 写成论文清单、年份线或先射箭再画靶 |
| `04_学习向导.md` | 建立学习桥 | 知道怎样从基础课走到目标论文、核心图和进组前问题 | 写成通用课程表，或把 `03` 换口吻重写 |

当前状态：

- S4 已进入冻结候选稿，不要称为正式冻结；保存于 `112de06`。
- S4 已完成图文审查、小白读者回放和 P1/P2/P3 回灌：范围图与层级树边界、真实论文图标注规则、证据虚线与学习回看线分离、最小矩阵用法、填写后的视觉支撑规格卡。
- 最近 S4 机械验证通过：`generate_assets.py`、8 张 `s4-*.svg` XML 解析、`git diff --check`、旧误导关键词检查。
- 浏览器截图可以用，但不能抢前台。除非已经确认完全后台或隐藏窗口方式，否则不要启动会闪到前台的 Edge / 浏览器截图流程。

下一步只做 `M0` 反补：修改 `quality-workbench/支撑方法/从资料堆到理解路径.md`，把 S1-S4 已稳定的执行动作、证据边界、学生输出、关系外部化、图文消费、停写条件和回退路径吸收进去。不要直接进入 `D00-D04`、真实样本、skill、verifier 或 Harness。

项目迁移、旧路径和历史复盘放在后文冷区；新窗口通常不需要先读。

## 当前稳定入口

新窗口或上下文压缩后按这个顺序恢复：

1. 读 `AGENTS.md` 和 `QUICKSTART.md`。
2. 读本文件，确认当前分支、样本状态、workbench 状态和下一步。
3. 涉及导师调研、00-04 成品或阶段 2-4 修订时，读 `.agents/skills/research-advisor/SKILL.md`。
4. 只有执行阶段 1 技术步骤时，再读 `src/phase1/pipeline.md` 和必要 references。
5. 运行 `git status --short -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**'`，确认只处理相关文件。
6. 对非小任务写任务合同，`隔离审查` 必须填 `不开 / 需要 / 延后`；命中 S4、D03/D04、真实成品、长上下文、质量不可信或图文/证据风险时，默认需要或延后 subagent。

不要从旧 e2e 记录、旧 quickstart、旧目标模式或已删除的兼容脚本反推当前 workflow。当前 Phase 1 入口只认 `phase1_init.py + --prof-dir` 的逐步执行路径。

`docs/e2e/` 只作历史回归记录，`docs/batches/` 只作官方输入清单；二者都不是执行流程说明。

当前文档设计讨论期间，`quality-workbench/` 是 `00-04` 文档标准和支撑方法的设计源头。根部只放 README；五份文档标准放在 `quality-workbench/文档标准/`，学习认知、信息检索和证据筛查等跨文档方法放在 `quality-workbench/支撑方法/`。若它与旧模板、旧 skill reference、旧 verifier 或 `docs/计划书.md` 的旧段落冲突，先以 workbench 为准，再决定是否统一回收正式 Harness。

## 单窗口失控处理规则

这轮工作已经证明，一个窗口长时间连续推进会带来上下文腐烂、任务漂移和“看起来一直在干活，实际越跑越偏”。以后不要再让一个窗口长期无人值守式推进。

出现下面任一情况，就开新窗口：

- 当前窗口已经发生上下文压缩，还要从调研转到写作、从写作转到审查，或从方法转到样本。
- AI 两次以上用解释、道歉或原则承诺回应，但没有新增可检查验收。
- 同一窗口连续改过方法论、文档标准、样本、skill、verifier 或 harness 多个层级。
- 用户开始看不清它到底改了什么、如何验证、哪里还没证明。
- 任务已经从“做一个最小改动”变成“顺便把整个系统整理好”。

以后按多窗口角色推进，但默认只保留两个必要的主流程窗口；这不限制在具体任务里临时开 subagent：

| 窗口 | 职责 | 是否改文件 | 退出条件 |
|:---|:---|:---:|:---|
| 执行窗口 | 只做一个层级的一个任务 | 可以改 | diff 可审、验证完成、提交或说明未提交原因 |
| 审查窗口 | 只读审查 diff 和文档质量 | 默认不改 | 输出问题清单和证据，不顺手修 |

主控不是常驻第三个窗口。只有当执行和审查结论冲突、任务跨多个层级、或需要整理下一轮路线时，才开一个短的主控窗口，用来定目标、锁层级、合并判断和更新交接。

调研窗口和回归窗口也不是默认常驻角色。需要外部资料时开调研窗口，只交付“资料到判断”；需要跑样本、verifier、渲染或 smoke 时开回归窗口，只交付结果和失败原因。这里的“窗口”指长期承载上下文的主对话，不等于 subagent 数量；subagent 是短时并行侧线，用完把结论回灌后就关闭。

核心规则：一个窗口只解决一个层级的一类问题。跨层问题写成后续回收，不顺手修改。

高风险文档不要只靠主窗口自审。本项目已授权在高风险开发和审查中主动使用 subagent；这条授权对“继续修 S4 / D03 / D04 / 真实样本”这类短指令同样生效。`00-04` 成品、`D00-D04` 标准、`S4` 可视化、`03` 论文路线、`04` 学习向导、长上下文后的重构和用户已经指出质量不可信的文件，主窗口完成自审后应开只读 subagent 做“小白读者回放”或专项审查。需要读很多文件或来源时，可先开探索型 subagent；需要跑长验证时，可开验证型 subagent。Subagent 只接收冻结稿、读者身份、预期出口和禁止事项，不接收完整聊天记录和作者辩解。主窗口必须把 subagent 实际读到的路线与原设计路线对照，再决定删、改、拆、降级或回到上游方法。

短指令默认分派：

| 用户短指令 | 主层级 | 默认 subagent | 先做什么 |
|:---|:---|:---|:---|
| 继续修 S4 | `S4 可视化与理解` | 图文审查；必要时加小白读者回放 | 先只读判断 S4 是否真的回答“什么情况下应该出什么图”，不要直接编辑 |
| 继续修 D03 / 03 论文路线标准 | `D00-D04 / D03` | 小白读者回放；涉及论文证据时加证据审查 | 先判断学生能否复述论文问题链，防止先射箭再画靶 |
| 继续修 D04 / 04 学习向导标准 | `D00-D04 / D04` | 小白读者回放；涉及目标论文或课程桥时加证据审查 | 先判断学生能否从基础课走到目标论文和下一步行动 |
| 继续修真实样本 | 真实样本 | 小白读者回放；身份、论文、领域或目标论文风险加证据审查；图文密集处加图文审查 | 先确认样本路径和本轮只修哪份或哪类问题，不一次重写五份 |

## 硬约束

- 不读取或引用 `archive/`，包括 `output/**/archive/**`。存档只供人工溯源。
- archive 禁读指 Agent 不得手动打开、搜索、复制或引用历史/中间文件；脚本通过 `--prof-dir` / `ProfDirResolver` 读取当前 active `_internal/archive/<ts>` 不属于手动读取 archive。
- 不提交 `.codex/` 或未明确纳入项目的本地目录，例如 `精髓转移/`。
- 导师成品根目录只能暴露 `00_材料导读.md`、`01_基础画像.md`、`02_领域地图.md`、`03_论文路线.md`、`04_学习向导.md` 和 `_internal/`。
- 来源必须可追溯；缺失写 `[未找到]`；弱推断写 `需人工复核`。
- 不做导师评价，不写匹配度、推荐申请、是否值得报考等判断。
- 每个里程碑只解决一类问题：成品质量、证据可靠性、教学设计、可视化、端到端自动化、项目瘦身或 verifier 底线。
- verifier 只做 smoke，不冒充学术质量。学术质量来自证据、蓝图、自审、回归和人工反馈。

## 已稳定的 harness 设计

### Fact Pack

阶段 1 仍是有价值的事实层资产：`phase1_init.py`、Google Scholar / OpenAlex / arXiv 采集、`risk_gate.py`、受控身份修正、受控论文剔除、`render_profile.py` 和 `verify_profile.py`。

新导师从三行输入初始化：

```bash
python src/phase1/phase1_init.py \
  --university "<大学>" \
  --institute "<学院或研究所>" \
  --department "<部门>" \
  --name "<中文名>" \
  --official-url "<导师官网 URL>"
```

后续统一使用脚本输出的 `prof_dir` 和 `--prof-dir` 参数。不要手动拼 `_internal/archive/<ts>`。

### Cognitive Blueprint

每位导师在写 `02-04` 前必须维护 `_internal/blueprint.md`。蓝图不是过程日志，而是认知设计合同，至少覆盖：

- 读者起点
- 导师当前方向一句话
- 目标论文及选择理由
- 核心图或图组
- 平台链路
- 课程到论文的学习桥
- 可视化计划
- 证据风险和人工复核

蓝图薄弱时先继续检索和核证，不要继续润色正文。成品正文必须消费蓝图，而不是只让 verifier 看到蓝图字段存在。

### 成品质量底线

每份成品文档都按“小白执行 AI + 小白学生读者”设计，必须自包含：说明本文解决什么问题、为什么这样读、读完应获得什么能力；关键术语和缩写首次出现时展开；不能假设读者读过项目规则、聊天记录或其他导师样本。

可视化是理解工具，不是装饰。不要把所有内容画成 Mermaid；领域定位优先层级树或定位表，论文路线优先问题-方法-角色矩阵，学习向导优先课程-能力-论文桥接表，真实流程或平台链路才用小型 Mermaid 或文本链路。

### 讨论即沉淀

当前进入逐文档、逐小节的人工式质量审查。顺序是先讨论整份文档职责和大纲，再讨论小节正文逻辑、图文配合和出口能力；单份文档完成后回看整篇衔接，五份文档完成后回看 `00-04` 整体认知阶梯。

每次用户和 AI 讨论出稳定结论后，先判断它处在哪一层。属于五份成品结构和章节逻辑的，先进 `quality-workbench/文档标准/`；属于学习设计、信息筛查、证据分级、图文表达等跨文档方法的，先进 `quality-workbench/支撑方法/`。当前阶段不要把每个小反馈立刻升格到正式 Harness。只有跨导师可复用、能稳定提高质量、且归属明确的结论，才在统一回收时进入 `docs/计划书.md`、`.agents/skills/research-advisor/references/`、`assets/templates/`、verifier 或 tests。

`quality-workbench/文档标准/00_材料导读.md` 已完成第一轮结构设计并提交为 `b77c4ed3 docs: finalize material guide workbench`。当前五节结构是 `材料定位`、`阅读顺序`、`读后自检`、`来源核查`、`参考资料`；文首有目录和显式 anchor；底部保留 `已确认规则`、`成品试写时验证`、`后续落地清单`。早期命名和入口结构不再作为当前 `00` workbench 结构。

当前协作模式是用户实时反馈、AI 逐步讨论和修改；旧的“长时间无人值守自进化”目标只作历史背景，不再作为默认工作方式。当前不要继续 `D00-D04` 文档标准，也不要直接重构正式模板或 verifier；先完成 `S1-S4` 支撑方法，当前优先处理 S4。

## 当前 workbench 风险

`quality-workbench/` 仍是当前设计源头。正式 Harness、模板、skill reference 和 verifier 只能在工作台结论稳定后统一回收。

当前层级命名：

```text
M0 总方法：quality-workbench/支撑方法/从资料堆到理解路径.md
S1 信息检索与筛查：quality-workbench/支撑方法/信息检索与筛查.md
S2 学习与认知原则：quality-workbench/支撑方法/学习与认知原则.md
S3 学术品味与文稿设计：quality-workbench/支撑方法/学术品味与文稿设计.md
S4 可视化与理解：quality-workbench/支撑方法/可视化与理解.md
D00-D04 文档标准：quality-workbench/文档标准/00_材料导读.md 到 04_学习向导.md
```

最近的工作集中在四份支撑方法，尤其是 S4。迁移后的提交 `b7bdbb6 docs: ground visualization method in reviewable cases` 曾把 S4 改成“关系负担 + 八问验收 + 真实 `00-04` 场景试压”，并加入多张局部 SVG：

```text
quality-workbench/支撑方法/_assets/visualization-guide/table-matrix-use.svg
quality-workbench/支撑方法/_assets/visualization-guide/evidence-boundary-use.svg
quality-workbench/支撑方法/_assets/visualization-guide/range-position-use.svg
quality-workbench/支撑方法/_assets/visualization-guide/paper-role-use.svg
quality-workbench/支撑方法/_assets/visualization-guide/route-semantics-use.svg
quality-workbench/支撑方法/_assets/visualization-guide/reading-decision-use.svg
quality-workbench/支撑方法/_assets/visualization-guide/relationship-burden-gate.svg
```

这些 SVG 曾经过 XML 解析和 Edge 渲染检查。检查只能说明文件可渲染，不能证明 S4 的方法质量已经符合用户要求。

### S4 当前中间态

2026-07-08 的 S4 已经从只读审查进入重构中间态，尚未提交。用户明确纠偏：S4 不能再写成“尽量少用图”，也不能继续把表格、字段、矩形和流程图当成主要可视化能力。S4 要回答的是：

```text
教学和科研理解中，什么情况下应该用什么图；
怎样尽可能多用好图，让图帮助学生理解，而不是把文字硬塞进形状里。
```

当前未提交稿主要改了两块：

- `quality-workbench/支撑方法/可视化与理解.md`：标题改为 `S4 可视化与理解：把关系画到学生能用`，主线改为“学生输出 -> 关系负担 -> 图像语法 -> 视觉语义边界 -> 图文消费 -> 输出回放”。新增图像语法：地图 / 区域图、层级图、概念图、机制示意、时间线 / 路线 / 阶梯、比较图、证据 / 不确定性图、真实论文图 / 标注图、数据图。
- `quality-workbench/支撑方法/_assets/visualization-guide/`：删除旧的 `*-use.svg` 七张矩形 / 流程 / 表格化示意图，新增 `generate_assets.py` 和八张新 SVG：`s4-visual-vocabulary.svg`、`s4-field-map.svg`、`s4-paper-role-map.svg`、`s4-mechanism-sketch.svg`、`s4-learning-bridge.svg`、`s4-evidence-uncertainty.svg`、`s4-annotated-paper-figure.svg`、`s4-comparison-diagram.svg`。

已做的机械验证：

```text
python quality-workbench/支撑方法/_assets/visualization-guide/generate_assets.py
XML 解析：8 张 s4-*.svg 通过
git diff --check -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**' 通过
Edge headless screenshot：8 张 s4-*.svg 均能生成 PNG
```

验证边界必须写清：这些检查只证明 SVG 能生成、能解析、能渲染，不证明图真的帮助教学理解。

已完成一次图文审查 subagent，只读范围为 S4 和 S4 assets。图文审查结论不能当润色意见，必须当结构性返工点处理。2026-07-08 当前未提交稿已经先处理最高优先级问题：

- `s4-evidence-uncertainty.svg` 已从“弱线索 -> 交叉证据 -> 直接证据”的推进图改成三张同尺寸证据边界卡，分别写清“可支撑什么 / 不能支撑什么 / 下一步复核什么”；图形谱系里的证据图小图标也同步改成同尺寸边界卡。
- `可视化与理解.md` 的证据段已补成图前卡点、图下读法和图后消费：弱线索只进复核清单，不能进入范围图、角色图或机制图。
- 六个核心示范已把“正文消费”改为可复刻的图文块，分别写清图前、图下、图后。
- 层级图和数据图暂不新增独立示范：层级图先在主决策表约束上下位关系，数据图只有真实数据能改变阅读判断时才出现。
- `s4-field-map.svg` 已改成阅读层级图，不再用导师点位、面积、距离或空间重叠暗示事实关系；图中明确允许读出和禁止读出的语义。
- 2026-07-08 随后修复了 SVG 里可见的重叠：`s4-annotated-paper-figure.svg` 改成长箭头之外的编号标注，`s4-visual-vocabulary.svg` 去掉穿过图标的中心放射线；后续 Edge 截图一律用隐藏窗口启动，避免干扰前台工作。

小白读者回放已完成。结论是：当前 S4 可以作为冻结前候选稿，但不建议直接冻结。新人已经能拿走五个动作：论文角色先做角色表，机制链先拆输入 / 处理 / 测量 / 输出，学习桥先看前置能力，弱证据退回证据边界卡，字段多时不上 SVG。回放指出的 P1/P2 已做一轮 S4 内部回灌：

- 范围图与层级树边界已补强：范围图只表达阅读入口层次，不等于学科上下位；真正的包含关系要走层级树或嵌套集合图。
- 目标论文核心图标注已补“原图来源、图号或面板、引用 / 局部截取 / 重绘方式、标注层类型”等规格字段，SVG 里也增加图例。
- 证据虚线和学习回看线已分离：虚线保留给“弱线索 / 需复核”，机制图和学习桥改用无箭头灰色回看线。
- 字段很多时仍不上 SVG；当两个判断维度共同起作用时用矩阵。S4 已补一个 `论文 × 角色 / 证据等级` 的最小矩阵用法，不新增矩阵 SVG。
- 已补一张填写后的“视觉支撑规格卡”样例，用 `03_论文路线.md` 的论文角色概念图说明：先做角色表，再判断是否需要进一步画图，并写清证据允许的边、禁止语义、图前 / 图下 / 图后消费和退回方案。

当前 S4 可以进入冻结候选自查，但还不能说已经正式冻结。最后确认只看两件事：

```text
机械验证：generate_assets.py、XML 解析、git diff --check、关键词检查、git status。
人工边界：机械验证只能证明格式和资产可生成，不能证明教学质量最终成立。
```

本轮仍不碰：`archive/`、D00-D04、真实导师样本、skill、verifier、harness。

当前项目最大风险在于执行质量：方法写得像原则，落地写成板块，图表变成装饰。反复出现的失败包括：

- 支撑方法讲原则，自己却没有从真实问题走到可执行动作。
- 参考资料放在文末，正文看不出资料怎样改变判断。
- 文档标准像分类抽屉，一节讲一个板块，没有上一节交给下一节的链条。
- `03_论文路线.md` 最容易先射箭再画靶：先拿 `02` 的领域判断当靶子，再挑论文支撑它。正确做法是回到相关履历阶段的论文集合，尽量完整分析论文内容，再归纳关系。
- 可视化最容易变成图形配额：把表格包装成图，把平台、时间、学习都画成相似流程，把论文角色和证据边界都画成节点连线。
- AI 遇到反馈后容易承认问题，再做局部修补，但没有把反馈变成新的验收用例。

后续每次修订都要把用户反馈转成可失败、可检查的门：

```text
这次要阻止什么具体失败？
为什么之前会失败？
资料或项目经验支持哪个判断？
执行者下一次要做什么不同动作？
审稿人怎样看出它没有再犯？
这条规则暂时不覆盖什么？
```

## 当前样本状态

### 张鹏举

位置：

```text
output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举
```

状态：

- 已有 `00-04` 五份成品文档。
- 已有 `_internal/blueprint.md` 和 `_internal/evidence/key_claims.md`。
- 当前目标论文是 2025 年液体水 ICD 竞争机制论文，回看论文是 2022 年液体水 ICD 首次观测论文。
- `04_学习向导.md` 已从高数、线代、普物接到光电子谱、液体水 ICD、核心图、平台链路和进组问题。
- `02_领域地图.md` 已补“从领域地图接到目标论文”，把目标论文、回看论文、核心图、平台链路和学习桥显式接到 `03/04`。

剩余风险：

- 固体/关联电子材料分支公开论文链较短，只能作为正在形成的分支。
- 2026 年 SF6 论文存在单篇 DOI 元数据 ORCID 差异，不能改写主体身份，仍需人工复核。
- 机械验证通过不代表 2025/2022 液体水 ICD 的所有图和模型解释已经完成专家级学术审查。

## 最近上下文里程碑

这里只保留会影响当前协作方向的近况；历史提交、验证细节和旧回归记录用 `git log`、`git show <hash>` 与 `docs/e2e/` 追溯，不在本文件继续堆长流水账。

- `b77c4ed3 docs: finalize material guide workbench`：`quality-workbench/文档标准/00_材料导读.md` 第一轮结构完成。当前 `00` workbench 以 `材料定位 / 阅读顺序 / 读后自检 / 来源核查 / 参考资料` 为五节结构，并保留成品试写验证点和后续落地清单。
- `1fbe7f55 docs: define material guide references`、`7951f6f1 docs: define material guide source checks`、`280fd1aa docs: define material guide self check`、`a759ad4b docs: add learning immersion principles`：这些提交记录了 `00` workbench 与学习认知原则的近期讨论沉淀。
- `f3243a0c docs: update agent working agreements`：`AGENTS.md` 已把当前工作重心写成“先讨论成品质量，再反推 Harness”。当前 `quality-workbench/` 已整理为 `README.md + 文档标准/ + 支撑方法/`。
- Phase 1 入口、`--prof-dir`、三行最小输入、archive 禁读、五份交付物口径等工程约束已在 `AGENTS.md`、`QUICKSTART.md`、`src/phase1/pipeline.md` 和相关 tests 中收敛。不要从旧 e2e 或旧提交描述反推当前执行路径。

当前仍未回收的旧实现：正式 `phase0-material-guide.md`、`assets/templates/00_材料导读.md` 和 `verify_phase_docs.py` 仍可能保留早期结构。它们暂时视为旧实现，不反过来约束 workbench；等 `00` 真实成品试写验证后再统一回收。

## 当前工作树注意

当前分支是 `codex/learning-folder-guardrails`。`git status --short --branch -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**' ':(exclude)**/_internal/archive/**'` 仍显示下面这些未提交内容：

```text
M AGENTS.md
M QUICKSTART.md
M output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举/00_材料导读.md
M quality-workbench/README.md
M quality-workbench/支撑方法/AI协作开发与质量控制.md
M quality-workbench/支撑方法/从资料堆到理解路径.md
M quality-workbench/文档标准/00_材料导读.md
M quality-workbench/文档标准/01_基础画像.md
?? 张鹏举学习_学习与认知原则_48abb85_已移出学习区.md
?? 张鹏举学习_项目回收备忘_已移出学习区.md
```

处理方式：

- 上面这些不是 S4 里程碑的暂存遗漏。新窗口不要为了“清理状态”直接提交或回滚。
- 如果继续 M0，只允许处理 `quality-workbench/支撑方法/从资料堆到理解路径.md`，并在提交时只暂存 M0 相关 diff。
- `output/.../张鹏举/00_材料导读.md`、`quality-workbench/文档标准/00_材料导读.md` 和 `quality-workbench/文档标准/01_基础画像.md` 属于后续 D00-D04 / 真实样本层，不要在 M0 反补时顺手修。
- 两个张鹏举学习备忘录未明确纳入项目，不要读取、改写或提交，除非用户单独点名。
- `archive/`、`output/**/archive/**` 和 `**/_internal/archive/**` 仍禁止手动读取、恢复或提交。

## 下一步建议

下一窗口如果没有新的 S4 质量问题，优先进入 `M0` 反补，不要直接转入 D00-D04、真实样本、skill、verifier 或 harness。建议任务合同写成：

```text
当前层级：M0 总方法论
本次目标：把 S1-S4 已稳定的判断反补到 `从资料堆到理解路径.md`，尤其是 S4 的“关系外部化、图像语法、图文消费、输出回放”
输入和来源：S1-S4 当前稿、S4 图文审查和小白读者回放结论、quality-workbench/README.md、docs/上下文交接.md
本次不碰：archive/、D00-D04、真实导师样本、skill、verifier、harness
完成条件：M0 不再停留在“资料到理解”的空泛链条，而能吸收 S1-S4 的可执行动作、停写条件和回退路径
验证方式：git diff --check、关键词检查、主 agent 自查；如涉及大段重构，完成后开只读回放
人工审查边界：机械检查不证明方法论质量
隔离审查：延后；M0 形成冻结候选后做只读回放
```

具体顺序：

1. 先确认 S4 的最后机械验证是否通过；不要重新打开旧 SVG 或旧 S4。
2. 读 M0 当前稿，标出哪些段落仍像抽象总纲，哪些段落需要吸收 S1-S4 的执行动作。
3. 只在 M0 内部回收，不把规则提前写进 D00-D04 或 Harness。
4. M0 稳定后，再进入 D00-D04 文档标准。

截图注意：浏览器截图可以用，但不能抢前台。除非已经确认是完全后台或隐藏窗口方式，否则不要启动会闪到前台的 Edge / 浏览器截图流程。

新窗口不要把 SVG 数量当成质量。S4 的合格线是：新人能说出学生卡住的是范围、上下位、命题关系、机制、时间、依赖、比较、证据、论文图读法还是数据模式，并能据此选择图像形式、说明证据限制和正文消费位置。当前 S4 只能称为冻结候选稿；是否正式冻结，留给用户确认或 M0 反补前的最后一轮人工判断。
