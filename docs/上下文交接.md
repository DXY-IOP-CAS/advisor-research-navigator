# pilot-test 上下文交接 — 2026-07-03 晚间交付版

## 现在的首要目标

用户还有约 2 小时要提交 `docs/计划书.md` 中定义的四份最终文档。不要继续做大规模 harness 重构，也不要再跑完整 subagent 端到端测试。

四份文档是：

1. `01_基础画像.md`：导师基础画像 + 按学术履历分组的论文列表
2. `02_领域脉络.md`：子领域发展树、里程碑、当前前沿
3. `03_论文定位.md`：每篇论文在领域树上的位置标注
4. `04_学习讲义.md`：从高数 + 线代到前沿的递进学习路径

当前应直接进入阶段 2/3/4 产出设计和写作。阶段 1 harness 已经足够支撑当前交付，不要继续追求完美。

## 新窗口第一动作

1. 读本文件。
2. 读 `docs/计划书.md` 的第 2 章，尤其是 2.1 和 2.4。
3. 读 `src/phase1/pipeline.md`，只看当前 Phase 1 技术入口和输出结构。
4. 不读 `archive/`。AGENTS.md 明确规定 archive 只写不读。
5. 看当前最终可用画像路径，再决定从哪位老师开始写 02/03/04。

建议命令：

```bash
git status --short -- . ':(exclude)archive/**' ':(exclude)output/**/archive/**'
git log --oneline -n 8
```

如果要做前置检查，只做确定性 smoke，不跑 subagent：

```bash
python src/phase1/verify_profile.py --prof-dir "output/<大学>/<学院或研究所>/<部门>/<姓名>"
python src/phase1/risk_gate.py --prof-dir "output/<大学>/<学院或研究所>/<部门>/<姓名>"
```


## 2026-07-03 checkpoint：张鹏举阶段 2-4 写作口径

本轮主输入已锁定为 `output/中国科学院大学/中科院物理研究所/超快物质科学中心/张鹏举/01_基础画像.md`。旧版 `output/中国科学院大学/中科院物理所/超快物质科学中心/张鹏举/` 已移动到顶层 `archive/旧版产出/`，避免后续检索时混入旧噪声。除归档动作外，Agent 仍不得读取 `archive/`。

阶段 2-4 的核心目的不是并列生成三份报告，而是服务申请学生从小白过渡到可能进组后的当前研究领域。阶段 2 要先短暂回顾导师学术路径，帮助学生建立人物和时间框架，然后重点收敛到张鹏举当前在物理所超快物质科学中心最可能开展的方向：阿秒/极紫外光源、超快光电子谱、液相和凝聚相超快激发态动力学。早期原子分子碰撞、ICD 等方向作为当前方向的前史和能力来源处理，不平均展开成百科。

阶段 3 在阶段 2 的领域框架上重新定位论文，区分当前主线、技术前身、早期背景、旁支或弱证据论文。阶段 4 再把前三段反向翻译成学习路径，从高数、线代和本科物理出发，抽取读懂当前方向需要的知识点，形成个性化小字典。三份文档都必须避免导师评价，不写推荐/匹配度，不捏造来源；来源缺失写 `[未找到]` 或“需人工复核”。
## 当前 git 状态

最新关键提交：

- `4bd5ecd feat(phase1): add risk-gated search strategy`
- `84e0033 docs(skill): tighten research advisor workflow`
- `64f1630 fix(phase1): align minimal input and render output`
- `91939e6 docs(e2e): record minimal prompt zhangpengju run`

当前已知未跟踪项：

- `output/中国科学院大学/中科院物理研究所/...`：上次 e2e subagent 用“中科院物理研究所”生成的重复路径证据目录。不要混入提交；也不要现在清理，避免节外生枝。

注意：本仓库实际 `.git` 在上级目录，受 Codex sandbox 影响，git add/commit 可能需要 escalated permission。

## 刚完成的 harness 决策

新增了 `src/phase1/risk_gate.py`，解决“什么时候 standard、什么时候 conservative 多元检索”的判断问题。

规则：

- 默认永远是 `standard`。
- 跑完三源合并后执行：

```bash
python src/phase1/risk_gate.py --prof-dir "output/..."
```

- 输出 `mode: standard`：继续 render / verify / 写后续文档。
- 输出 `mode: conservative_required`：只按 reason 定向补搜，不要凭感觉多轮乱搜。

策略文档：`docs/phase1运行策略.md`。

## 不要做

- 不要再跑完整 subagent 端到端测试。现在时间目标是提交四份最终文档，不是继续验证 harness。
- 不要读 `archive/`。
- 不要继续改 prompt 强度、写更多空泛 skill 规则。
- 不要把 `verify_profile.py` 通过当成内容质量完全通过；它是结构门控，不是最终内容审稿。
- 不要做导师评价，比如“匹配度”“值得报考”“推荐申请”“影响力大”。
- 不要为了补齐而捏造来源；缺失就写 `[未找到]` 或“需人工复核”。

## 接下来 2 小时执行建议

### 0-15 分钟：选定输入和 smoke

- 选一个当前最可靠的 `01_基础画像.md` 作为阶段 2-4 输入。
- 跑 `verify_profile.py --prof-dir ...`。
- 跑 `risk_gate.py --prof-dir ...`。
- 若 risk gate 要 conservative，先记录风险；除非风险会直接污染 02/03/04，否则不要大规模重搜。

### 15-55 分钟：写 `02_领域脉络.md`

目标不是百科综述，而是为阶段 3 提供领域树：

- 识别老师深耕的 1-2 个子领域。
- 写清领域起点、关键问题、主要技术路线、里程碑、当前前沿。
- 每个判断尽量绑定来源 URL；缺失标 `[未找到]`。
- 不评价导师，只描述方向。

### 55-95 分钟：写 `03_论文定位.md`

基于 `02_领域脉络.md` 反看 `01_基础画像.md` 的论文列表：

- 每篇论文标注到领域树节点。
- 标出阶段转型点：研究对象变化、方法变化、合作网络变化。
- 对单源 OA/arXiv 或 risk gate 提醒的论文，标为“需人工复核”，不要把它当强证据。

### 95-115 分钟：写 `04_学习讲义.md`

从高数 + 线代起步，输出“个性化小字典”：

- 数学基础：哪些概念必须补。
- 物理基础：哪些本科物理直觉必须补。
- 高阶延伸：哪些主题只需按需查，不要装成完整教材。
- 路径要服务于读懂该导师方向，不要写通用课程大纲。

### 115-120 分钟：最终检查和提交

- 检查四份文件路径和标题。
- 检查来源 URL / `[未找到]`。
- 检查没有导师评价。
- `git status` 确认只提交本次交付文件。

## 推荐给新窗口的第一句话

复制这句话给新窗口：

> 读 `docs/上下文交接.md`，不要读 archive。现在目标是在 2 小时内基于现有 `01_基础画像.md` 产出计划书定义的 `02_领域脉络.md`、`03_论文定位.md`、`04_学习讲义.md`，必要时只做 `verify_profile.py` 和 `risk_gate.py` 的 deterministic smoke，不跑完整 subagent e2e。